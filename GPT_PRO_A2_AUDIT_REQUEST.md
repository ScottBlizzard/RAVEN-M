# Independent Audit Request: A2 Verified Progress Memory

Audit the current branch as a complete experimental packet. Do not inherit conclusions from deleted historical framework-selection materials.

## Required audit questions

1. Is A2 a targeted response to the frozen A1 evidence, or does it combine too many causal changes?
2. Does any active A2 decision use hidden AndroidWorld UI/evaluator information rather than the model-visible screenshot and task prompt?
3. Are A0, A1, and A2 paired fairly on model revision, decoding, instances, seed, observation, action protocol, budgets, and evaluator?
4. Is the one-state `observed / verified / pending / expected / visible outcome` memory well defined and actually injected without adding calls?
5. Is removing the structured A2 prefix from ordinary history scientifically defensible as memory deduplication, and how must it be described in attribution?
6. Is the cost guard conservative enough? Audit screenshot-state signature, coordinate equivalence, two-attempt threshold, two-block stop rule, waits/swipes/text actions, and false-block risks.
7. Are memory and guard effects separable from the saved L0–L5, request, response, action, transition, and evaluator records?
8. Do runner qualification, resume, invalidity, checkpoint, and aggregate paths preserve a valid 19-task scored suite?
9. Are tests and the zero-generation preflight sufficient before GPU use? Identify only checks that could realistically prevent an invalid or uninterpretable run.
10. Are the frozen success/cost/attribution gates appropriate for a one-seed summer-camp experiment?

## Output requirement

Return one Markdown document only. It must contain:

- verdict: `RUN`, `RUN AFTER REQUIRED FIXES`, or `DO NOT RUN`;
- a table of blocking, important, and optional findings with exact file/line references;
- exact required edits or patch-level specifications, not vague advice;
- a final frozen A2 experiment specification after those edits;
- the exact post-run analysis table and attribution checks;
- explicit statements of what A2 may and may not claim.

Do not redesign A2 into a planner/critic/RAG stack. Recommend the smallest corrections needed for a fair, interpretable, executable comparison. If no blocking problem exists, say so clearly instead of inventing work.
