# A1-R4 WRPL Primary Gate Result

A1-R4 stopped after the first fixed capability-gate task. `ExpenseDeleteMultiple2` was a scientifically valid failure: reward `0`, termination `max_steps`, 34 returned single-attempt model calls, 34 executed actions, and 134,776 total tokens.

The writer bootstrap solved the narrow A1-R3 nonactivation problem, but did not solve stale-state use. Five valid prefixes created semantic ledgers; the other 29 responses omitted the required prefix. The mechanism nevertheless injected semantic memory on 29 calls. After material screen transitions, the last valid ledger could remain active and guide later actions from an obsolete page context.

This evidence authorizes only one prospective change for A1-R5: retire the active semantic ledger when the current response has an invalid prefix and its executed action produces a material visible transition. The next request then receives only the frozen writer bootstrap. No historical A1-R4 artifact may be overwritten or relabeled.

Canonical machine-readable evidence is `A1R4_WRPL_PRIMARY_GATE_RESULT_2026-08-15.json`.
