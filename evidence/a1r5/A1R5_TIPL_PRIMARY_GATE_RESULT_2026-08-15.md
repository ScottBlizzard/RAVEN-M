# A1-R5 TIPL Primary Gate Result

A1-R5 stopped on the first fixed capability task with a scientifically valid reward-0 `max_steps` result: 34 calls, 34 actions, 134,566 tokens, and no transport retry.

The targeted R4 defect was repaired behaviorally. The run reached Expense Logs without returning to Home, invalidated stale ledgers 12 times, and deleted Public Transit. It still failed to delete Tuition Fees and Bike Repairs. The remaining failure is therefore no longer chiefly stale page context: the free-text ledger did not persist the immediate `open item -> press delete -> confirm -> verify absence` sub-obligation strongly enough, so the trajectory repeatedly revisited item details.

Any successor is a new prospective version. It may preserve a compact immediate confirmation sub-obligation, but may not rerun A1-R5, alter the native budget, or reinterpret this result as success.
