# A1-R10 PACP Primary Gate Result

A1-R10 failed the first gate with reward 0. The full-screen calibration was injected on 32 calls but did not repair spatial grounding and initially worsened app-icon targeting. Static prompt calibration is therefore insufficient; the final prompt-only test should require an explicit per-action percentage-to-coordinate self-check rather than add memory state.
