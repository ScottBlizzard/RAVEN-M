# A1-R12 CHP Primary Gate Result

A1-R12 produced a valid reward-0 max-step failure on `ExpenseDeleteMultiple2` (34 calls/actions). It collapsed 174 repeated ordinary-history entries across 34 prompt constructions, reducing total tokens from A1-R11's 141,660 to 135,473 (−6,187; −4.37%), but it did not improve reward.

This is a real cost effect, not a capability improvement. Together R9–R12 show that a detected loop, stronger recovery prose, static coordinate calibration, explicit coordinate self-check, and exact consecutive-history compaction can all activate without completing the task. The next intervention must change the decision process rather than add or rearrange prompt memory.
