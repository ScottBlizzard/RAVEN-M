# RAVEN-M Current Handoff — 2026-08-12

The comprehensive history remains in `HANDOFF_2026-08-11.md`. This document
records the subsequent A7 completion, A8-v2/A9 designs, initial live gates and
the next diagnostic/design campaign.

## Current verified status

- A0: 4/19, reward 4.5.
- A1: 5/19, reward 5.5; current best success count.
- A2: 0/19.
- A3/A4/A5: first capability task failed; terminal abbreviated controls.
- A6: 0/19, valid complete negative control.
- A7: 4/19, reward 4.0, 19 valid and 0 selected invalid. It is a transparent
  stitch of the original seven episodes and declared diagnostics, not a
  pristine single suite. See `evidence/a678/A7_TRANSPARENT_19_TASK_CONTROL.json`.
- A8-v2: initial prospective gate failed Expense, 0/1, 34 actions, 14 nonempty
  reads, max-step termination.
- A9: initial prospective gate passed Expense while memory stayed silent, then
  failed Retro after three canaries; 1/2, 0 invalid.

The compact live ledger is
`evidence/a678/A89_INITIAL_GATE_RESULTS_2026-08-12.json`.

## Authorized next execution after GPU activation

Run one fresh four-task diagnostic replication for each of `a8v2` and `a9`.
Each suite runs all four A0-success tasks even after reward failure. Do not
resume or overwrite the terminal initial suites. Do not run the remaining
fifteen tasks. The authoritative amendment is
`protocols/A89_FOUR_TASK_DIAGNOSTIC_REPLICATION_AMENDMENT_2026-08-12.md`.

Command shape:

```text
python implementation/scripts/run_a678_arm.py \
  --arm a8v2|a9 \
  --adb-path <frozen adb.exe> \
  --launch-receipt <fresh receipt bound to current preflight> \
  --four-task-diagnostic-replication \
  --execute
```

Run arms sequentially on one emulator. A fresh preflight and live receipt are
mandatory after any source-frozen change.

## GPT Pro design objective

Give GPT Pro `GPT_PRO_MEMORY_MECHANISM_DESIGN_REQUEST_2026-08-12.md` and this
repository branch. The next arm must remain one standalone memory mechanism:
no extra calls, planner, critic, verifier, hidden UI, guard or action override.
The target is a 4/4 A0 preservation gate followed by at least 6/19 and reward
greater than 5.5.

No agent may promise this outcome in advance. The proper deliverable is a
mechanism whose causal thesis, implementation and falsification plan make the
claim genuinely testable.

## 2026-08-13 A10-v2/A11 qualification outcome

A10-v2 and A11 have now been implemented, audited, and replayed with zero
generation calls against the materialized 27-episode corpus (1,668 verified
files, 442,138,413 bytes). Both are scientifically blocked before live use:

- A10-v2: 0/23 timely eligible A6 reads and 0/23 timely T1/T2 reads; A9 exposure
  or trigger-kind gate also fails.
- A11: 5/23 qualified A6 branch-pair segments (21.739%); independent A8-v2
  Expense and A9 Retro gates fail. Its competent-history sparse gate passes
  with one read, showing that the remaining problem is low recall/timing rather
  than uncontrolled injection.

The reports are `evidence/a10_v2/A10_V2_OFFLINE_REPLAY_REPORT.json` and
`evidence/a11/A11_OFFLINE_REPLAY_REPORT.json`. Passing software tests is not a
license to run either arm. Their formal no-GPU preflights stop only at the failed
real replay, and no live receipt was issued.

The next external design request is
`GPT_PRO_A12_MINIMAL_MEMORY_DESIGN_REQUEST_2026-08-13.md`. It asks GPT Pro for
one minimal high-recall action-divergence mechanism, or an evidence-based NO-GO,
and explicitly prohibits another unbounded synthesis of the A10-v2/A11 state
machines.
