# RAVEN-M 研究假设、现有证据与实验方向审计

> 用途：供 GPT Pro 或其他研究者进行独立、批判性的科研复核  
> 日期：2026-08-03  
> 项目目录：D:\ZJU\Summer_Camp\RAVEN-M-Research  
> 文档性质：研究判断与实验设计草案，不是项目既有结论，也不修改任何既有代码、协议或实验记录

---

## 0. 如何阅读这份文档

本文不是为 RAVEN-M 辩护，而是回答一个更重要的问题：

> RAVEN-M 当前声称解决的问题，在 AndroidWorld 的真实任务中是否真的经常出现？现有机制是否以合理成本解决了它？如果没有，应该缩小、重构还是更换研究问题？

为了避免把观点写成事实，全文使用四种证据标签：

- **[已证实事实]**：可以由代码、正式协议、实验报告或论文原文直接核对。
- **[观察性证据]**：从现有日志统计或案例审查中得到，但样本有限，不能直接推广。
- **[研究推断]**：由事实推出的合理解释，仍可能被新实验推翻。
- **[实验建议]**：尚未实施的研究设计。

本文希望 GPT Pro 做的是“审稿人式复核”，而不是继续替当前框架补规则。请尤其检查：

1. 研究问题是否真实存在；
2. 基准是否能暴露这一问题；
3. 方法的关键模块是否与问题一一对应；
4. 收益是否大于动作、调用、上下文和时间成本；
5. 当前结果是能力提升、鲁棒性提升，还是只证明系统更保守；
6. r1–r78 的反复调试是否已经造成明显的任务特定过拟合。

---

## 1. 一句话结论

目前最稳妥的判断是：

> **GUI agent 确实需要记忆；但现有证据尚不能证明，在标准 AndroidWorld 协议下，“记忆会自然变旧、互相矛盾或不可信”是一个足够高频、足够关键的主要瓶颈，也不能证明 RAVEN-M 当前较重的可靠性框架能够以净正收益改善任务成功率。**

更具体地说：

- **[已证实事实]** AndroidWorld 每个 episode 都重新初始化任务与 agent；RAVEN-M v2.2 又明确将记忆限制为 episode-local。标准实验因此几乎不会自然出现“保存三个月后因 App 更新而过时”的跨会话记忆。
- **[观察性证据]** 已有 Hard 日志的主要失败模式是控件定位、循环、上下文过长、动作不被支持、规划偏移、完成闭环失败，而不是旧记忆误导。
- **[观察性证据]** RAVEN-M 的冲突检测在现有自然日志里多数抓到的是兼容性转述，而非真实矛盾；其严格完成门控还造成过完成死锁。
- **[已证实事实]** 在 r56 Gate E 的同 seed、同任务配对实验里，简单摘要基线 B3 为 4/4，完整 RAVEN-M M0 为 3/4，同时 M0 使用更多动作、调用和 token。
- **[已证实事实]** r77 在 H17 上取得过 reward 1.0，证明“同 episode 的证据路由机制能够完成该任务”；但 r61–r78 长期围绕同一个 H17 调试，r78 又失败，因此这不能证明普遍泛化，也不能证明完整可靠性生命周期是成功原因。
- **[研究推断]** 当前最值得保留的核心，可能不是“普遍怀疑每条记忆”，而是：**结构化保存任务关键事实、绑定可检查证据、持续维护未完成目标，并仅在高风险决策处验证。**
- **[实验建议]** AndroidWorld 可继续用于测试 episode 内的事实保持、目标跟踪和证据闭环；跨 session 长期记忆与陈旧性应转到 MemGUI-Bench 或另建受控跨版本实验，不能把两种主张混在同一个结论里。

---

## 2. 原始研究问题到底是什么

RAVEN-M 想解决的直觉可以通俗地表述为：

1. GUI agent 会把过去看到的页面、执行过的动作和得到的结果存进记忆；
2. 这些记忆可能被误读、错误归因、过时或彼此冲突；
3. 如果 agent 无条件相信记忆，就可能做错动作或错误宣布任务完成；
4. 因此需要为记忆建立来源、状态、验证和使用权限；
5. critic 或 guard 在高风险位置阻止未经证实的信息驱动动作。

这个直觉本身合理，但它包含了几个必须分开验证的命题。

### 2.1 五个不能混在一起的命题

| 编号 | 命题 | 需要什么证据 |
|---|---|---|
| C1 | GUI 长任务需要记忆 | 去掉记忆后任务成功率或信息保持显著下降 |
| C2 | 自然产生的记忆错误足够频繁 | 真实轨迹中存在可量化的错误记忆机会 |
| C3 | 这些错误会实质伤害任务 | 错误记忆与失败动作、错误答案或错误完成有因果联系 |
| C4 | RAVEN-M 能识别并阻止这些错误 | 与简单摘要或普通结构化记忆相比，误用率下降 |
| C5 | RAVEN-M 的净收益为正 | 成功收益大于额外动作、调用、token、延迟和误拦截成本 |

现在最容易被支持的是 C1；C2–C5 仍缺乏充分证据。尤其不能用“我们成功拦住了人工注入的坏记忆”直接推出“真实 AndroidWorld 中存在大量坏记忆，因此方法能提高常规任务成功率”。

### 2.2 鲁棒性主张和能力主张必须分开

假设系统通过 20 个确定性 corruption fixtures，并把所有注入的错误记录都挡在 FACT 之外，这能证明：

> 当错误被按预设方式注入时，系统的某些不变量可以正常工作。

但它不能自动证明：

> 在未注入错误的自然任务里，这些错误经常出现；  
> 这些错误是基线失败的主要原因；  
> 完整框架能提高最终任务成功率；  
> 收益足以覆盖框架成本。

因此论文或答辩中应严格区分：

- **Capability / effectiveness**：自然任务上做得更好；
- **Robustness**：受到特定污染时下降更慢；
- **Inspectability**：能解释某条记忆为何被使用或拒绝；
- **Safety conservatism**：宁可不完成，也不让低证据结论通过。

这四者都有价值，但不是同一种贡献。

---

## 3. “点击右下角 +”为什么是关键反例

用户提出了一个非常重要的直觉：

> 我刚刚点击右下角“+”，马上看到“新建联系人”页面。这个事实在同一个任务、几秒钟内几乎可以完全相信，为什么还要一直怀疑或重新检查？

这个例子说明，“记忆可靠性”不能被处理成一个全局统一阈值。

### 3.1 这里实际上有四层不同事实

| 层次 | 示例 | 合理处理 |
|---|---|---|
| 动作事实 | agent 的确执行了点击 | 由执行器日志确定，无须语言模型重新验证 |
| 状态变化 | 点击后截图/UI tree 改变 | 由 before/after observation 确定 |
| 局部语义关系 | 当前页面中，“+”把当前状态带到新建联系人页 | 在当前 App、版本、页面状态下可给高置信度 |
| 全局任务完成 | 联系人已经成功保存到系统数据库 | 不能由“进入表单页”推出，必须做最终闭环验证 |

因此，正确的方法不是“所有历史都不可信”，也不是“所有已经发生的事情都绝对可信”，而是：

> **把不可争辩的事件记录、局部可迁移的程序知识、任务关键事实和最终完成证据分开管理。**

### 3.2 为什么局部事实可以高信任

在同 episode、极短时间尺度内，如果满足以下条件：

- 动作由系统执行日志确认；
- before/after 页面变化明确；
- 目标控件身份没有歧义；
- App、版本、页面状态未变；
- 该知识只用于可逆导航；

那么反复调用 critic 的预期收益通常很低。此时继续验证，往往只是用算力和时间重复确认已经确定的事实。

### 3.3 为什么仍不能把它无限泛化

“+ 曾经进入新建联系人页”不等于：

- 所有 App 的 + 都表示新建；
- 此 App 任意页面的 + 都表示新建联系人；
- 三个月后版本更新仍然如此；
- 点击后最终保存一定成功；
- 页面看起来进入表单就代表数据库已写入。

所以一条程序记忆应有明确作用域：

> App × 版本/页面签名 × 起始状态 × 控件身份 × 动作 × 观察到的后继状态。

作用域匹配时可以高信任；作用域不匹配时降级为探索提示，而不是直接删除。

---

## 4. 建议重新定义“记忆”

当前讨论中，“记忆”一词覆盖了太多性质不同的东西。建议至少拆成六类。

### 4.1 确定性事件日志

保存：

- 执行了什么动作；
- 动作参数；
- 动作前后截图或 UI tree 哈希；
- 时间；
- 是否发生可观察页面变化。

这类信息不是“模型猜测”，不需要再做信任分类。它应该是所有其他记忆的底层证据。

### 4.2 任务关键事实槽

例如：

- 人名、电话号码；
- 两个活动分别对应的 activity type；
- 收据中的金额；
- 一篇笔记里的精确句子；
- 用户要求中的必要字段。

每个槽应保存：

- value；
- entity / row identity；
- field name；
- acquisition step；
- source image / UI node；
- 是否由视觉直接读取、OCR 读取或模型推断；
- 当前 episode 是否仍有效。

这类记忆最容易出现“记住了值但绑定错对象”“只记住一个字段”“后面被相似值覆盖”的问题，也是长任务最值得研究的部分。

### 4.3 程序性提示

例如：

- 当前联系人列表页右下角 + 可进入新建联系人；
- 某页面需向左滑动才能看到下一项；
- 某 App 第一次保存时会要求选择账户。

它不应被当成永恒规则，而应带上适用范围。若当前页面签名匹配，可直接作为高置信度导航先验；若不匹配，则只作为探索建议。

### 4.4 目标账本

保存：

- 哪些子目标已完成；
- 哪些字段仍未获取；
- 哪些结果已经写入；
- 哪些完成证据还缺失。

很多所谓“记忆错误”其实是 goal drift：agent 找到部分信息后忘记了还要继续查询、跨 App 写入或最终回答。

### 4.5 失败缓存

保存：

- 在相同或相近状态下，某动作没有产生预期变化；
- 连续重复多少次；
- 是否可能只是网络、加载或临时 App 故障；
- 何时应允许重试。

失败一次不应永久宣布该方法错误。更合理的是短期降权、标为不确定，并在条件变化或有限次数后重试。

### 4.6 完成证据

完成不是普通记忆的一条标签，而是需要独立闭环：

- 创建联系人：保存后回到列表，并在列表或详情中看到新记录；高要求时再核对字段或底层 evaluator。
- 保存费用：列表中出现目标金额与类别；必要时重新打开详情。
- 回答问题：答案中的每个实体—字段关系都有可追溯证据。

这正是“动作步骤正确”和“任务最终完成”之间的区别。

---

## 5. AndroidWorld 与当前研究主张的匹配度

### 5.1 AndroidWorld 官方机制

**[已证实事实]** AndroidWorld 提供 20 个真实 Android App 上的 116 类可参数化任务，每个任务有自己的初始化、成功检查和清理逻辑。它适合评估 agent 是否能在动态环境中完成任务。

来源：

- [AndroidWorld 论文](https://arxiv.org/abs/2405.14573)
- [AndroidWorld 官方代码](https://github.com/google-research/android_world)

本地 vendored 代码也能核对以下行为：

- android_world/episode_runner.py 在每个 episode 前调用 agent.reset；
- m3a.py 的 reset 会清空 history；
- suite_utils.py 依次执行 task initialize、agent run、evaluator、tear_down。

### 5.2 RAVEN-M 协议进一步限制为 episode-local

**[已证实事实]** 当前项目的协议和内存模型明确限制跨 episode 访问：

- reports/method_and_system.md：每个 episode 从空 history / memory 开始；
- 04_protocols/experiment_protocol.md：每个 episode 使用空的 episode-local memory，Hard trajectories 不进入跨 episode 记忆；
- 05_project/src/raven_m/memory/models.py：禁止跨 episode 访问。

### 5.3 直接后果

**[研究推断]** 标准 AndroidWorld + 当前 RAVEN-M 协议天然适合研究：

- 同一长 episode 内的信息保持；
- 多页面之间的实体—字段绑定；
- 历史观察与当前屏幕的证据关系；
- 任务目标是否被持续维护；
- 完成声明是否闭环。

但它不适合直接证明：

- 三个月前的记忆因 App 更新而过时；
- 跨版本程序知识需要 supersede；
- 多次尝试之间的策略积累；
- 长期记忆的遗忘、更新、淘汰和迁移。

换句话说，当前系统中 stale、superseded、revoked 等生命周期状态设计得很完整，但标准协议几乎不给这些状态自然发生的机会。若论文以它们为核心贡献，评测环境与研究主张存在结构性错位。

---

## 6. 外部工作给出的直接信号

### 6.1 MemGUI-Bench：现有 GUI benchmark 的确缺少记忆任务

来源：

- [MemGUI-Bench 论文](https://arxiv.org/abs/2602.06075)
- [MemGUI-Bench 官方仓库](https://github.com/lgy0404/MemGUI-Bench)

**[已证实事实，按论文报告]**

- 现有移动 GUI benchmark 中，memory-related tasks 只占约 5.2%–11.8%；
- AndroidWorld 被统计为约 5.2% memory-intensive；
- MemGUI-Bench 包含 128 个任务、26 个 App；
- 89.8% 任务被设计为 memory-intensive；
- 平均 golden steps 为 36.2；
- 78.1% 涉及跨 App 信息传递；
- benchmark 支持跨尝试保持 agent state，并使用 pass@k 观察长期学习。

这恰好支持本审计的核心担忧：**AndroidWorld 能跑 GUI agent，但未必足以验证“记忆可靠性”是主要瓶颈。**

### 6.2 MemGUI-Bench 中自然出现的记忆问题是什么

论文中更常见的不是“三个月前 + 按钮位置变了”，而是：

- **partial memory hallucination**：同一任务中，记住一个股票价格却把另一个价格回忆错；
- **process memory hallucination**：已经找到信息，但忘记还要继续提取或写入另一个 App；
- **output memory hallucination**：导航和信息获取可能正确，但最终输出编码或检索错误；
- **实体—值误绑定**：记住值，却不知道它属于哪一行、哪只股票或哪个字段。

论文报告：

- M3A 去掉短期记忆后，SR 从 32.5% 降至 2.5%，IRR 从 35.1% 降至 0；
- Agent-S2 去掉长期记忆后，pass@3 从 45 降至 25，FRR 从 15.5 降至 9.1；
- 显式长期记忆使 Agent-S2 从 pass@1 27.3 提升到 pass@3 49.2，但伴随明显计算成本；
- 1,265 次执行中，72.3% 的失败是 timeout；
- 在 343 个非超时失败中，PMH、ProcMH 和 OMH 合计约占 58.9%。

这些结果支持“记忆很重要”，但更指向：

> 信息保留、实体绑定、目标持续和最终检索，比普遍的陈旧程序记忆更接近真实主要问题。

### 6.3 HAR-GUI：强制一直看历史可能反而变差

来源：

- [HAR-GUI arXiv](https://arxiv.org/abs/2511.09127)
- [HAR-GUI AAAI 论文](https://ojs.aaai.org/index.php/AAAI/article/download/40966/44927)

**[已证实事实，按论文消融]** HAR-GUI 研究 history-aware reasoning。其消融显示，仅通过 prompt 强制模型关注历史的变体可能比普通 GRPO 更差；更有效的是让模型学习何时、如何使用历史。

它支持一个重要设计原则：

> 记忆访问与验证应当是选择性的。始终把大量历史、规则和 critic 约束塞进上下文，可能损害当前屏幕理解和动作决策。

### 6.4 PG-Agent、AppAgent、Mobile-Agent-E 的位置

- **PG-Agent**：从轨迹构建页面图，重视经验组织与检索；核心问题更接近“如何复用成功导航经验”，而非记忆生命周期可信性。可访问[论文 DOI](https://doi.org/10.1145/3746027.3755189)与[官方代码](https://github.com/chenwz-123/PG-Agent)。
- **AppAgent**：[论文](https://arxiv.org/abs/2312.13771)。通过探索建立操作知识库，说明程序知识能帮助 GUI 操作。
- **Mobile-Agent-E**：[论文](https://arxiv.org/abs/2501.11733)。从成功与失败中形成 Tips / Shortcuts，强调跨任务学习和效率。

这些工作共同说明经验可以帮助 agent，但并未自动证明复杂的 FACT / HYPOTHESIS / ALERT / SUPPRESS 路由是最佳表达。RAVEN-M 若要形成清晰差异，必须证明：

1. 普通经验检索确实会因为证据权限不清而出错；
2. 可靠性治理能减少这种错误；
3. 减少错误后的净任务收益高于框架成本。

---

## 7. 当前 RAVEN-M 实验事实

### 7.1 r39 Gate E

本地报告：

> reports/protocol_v2_2_r39_gate_e_final.md

**[已证实事实]**

- B3：4/4；
- M0：3/4；
- M0 在 Expense 任务中把类别选成 Housing 而非 Donation；
- 相比 B3，M0 多使用约 42.86% model calls、76.86% prompt tokens、97.18% completion tokens。

这个结果不能支持“完整方法比简单摘要更好”。它最多说明，当时实现已经可以运行、协议能记录差异，并暴露了额外复杂度。

### 7.2 r56 Gate E

本地报告：

> reports/protocol_v2_2_r56_gate_e_final.md

共有 8 个配对的 non-Hard cells。

| 指标 | B3 简单摘要 | M0 完整 RAVEN-M | M0 相对 B3 |
|---|---:|---:|---:|
| 成功 | 4/4 | 3/4 | -0.25 |
| 平均动作数 | 9.25 | 11.75 | +27.03% |
| 平均调用数 | 14.25 | 19.75 | +38.60% |
| 平均 prompt tokens | 72,817.5 | 123,449 | +69.53% |
| 平均 completion tokens | 1,264.25 | 2,331.25 | +84.40% |

M0 的 Contacts 失败不是“错误记忆误导”：

- 姓名和电话已经正确填写；
- planner 自行增加了任务没有要求的 Company 字段；
- guard 拒绝写入虚构内容；
- agent 之后没有点击保存；
- 最终任务失败。

这说明 guard 的局部行为可能是正确的，但整个系统的净效果仍为负。**一个模块成功阻止坏动作，不等于完整系统成功。**

### 7.3 Frozen Hard breadth forensic analysis

本地报告：

> reports/breadth_forensic_analysis_2026-07-26.md

**[已证实事实]**

- 共 95 个完成 episode；
- 15 个因缺少 answer action 在结构上无法评分；
- 排除不支持任务后，成功 1/80；
- 59/80 个 supported cells 至少出现一次相同动作连续重复 3 次；
- 32/80 至少重复 10 次；
- 共 477 次动作后页面没有变化；
- M0 检测到 46 次循环，critic 全部建议 reobserve；
- reobserve 后 31.8% 的下一动作仍与之前完全相同；
- M0 为 0/19，全部耗尽预算；
- 与 B3 相比，M0 约多 33% 调用、53% token、52% wall time；
- 13 次完成尝试被 FACT gate 拒绝，形成完成死锁。

报告中的 M0 memory routes：

| 路由 | 次数 |
|---|---:|
| FACT | 150 |
| HYPOTHESIS | 2,777 |
| ALERT | 105 |
| SUPPRESS | 5,684 |

此外：

- 被引用的记忆中约 84% 是 HYPOTHESIS；
- 16 次引用记忆的 type_text 全部引用 HYPOTHESIS。

这暴露出一个内部张力：

> 如果 HYPOTHESIS 经常被引用并影响动作，它实际拥有多大权限？如果它没有权限，为什么把它塞入上下文？如果它有权限，FACT gate 的边界是否一致？

### 7.4 对 Hard 结果的解释

**[研究推断]** Hard 失败的主导因素更像：

1. 基础 GUI grounding 不稳定；
2. 相同动作循环；
3. 控件/坐标定位模糊；
4. 完成条件与 answer action 协议不匹配；
5. 长 prompt 和多 critic 导致上下文负担；
6. planner 产生任务外要求；
7. 守卫发现问题后只会 reobserve，却不给出有效恢复策略。

这些问题与“记忆是否陈旧”不是一回事。若基础 controller 尚不能稳定执行，继续增加可靠性规则可能只会让失败更可解释，却不提高完成率。

---

## 8. 自然 memory events 的只读审计

本轮对当前存档的 Hard M0 memory_events.jsonl 做了只读统计。由于包含正式记分轨迹和部分归档重试，数字与 frozen report 略有不同，不能混用为同一统计口径。

### 8.1 总体事件

共发现 23 个 memory_events.jsonl：

| 事件类型 | 数量 |
|---|---:|
| 总事件 | 10,884 |
| route | 8,959 |
| transition | 1,034 |
| write | 876 |
| contradiction | 15 |

route 分布：

| 路由 | 数量 |
|---|---:|
| SUPPRESS | 5,687 |
| HYPOTHESIS | 3,011 |
| FACT | 156 |
| ALERT | 105 |

write 分布：

| 写入状态/类型 | 数量 |
|---|---:|
| observed | 829 |
| candidate | 47 |
| episodic_fact | 776 |
| page_hint | 51 |
| failure | 49 |

### 8.2 生命周期事件几乎没有自然出现

在上述日志中：

| 生命周期事件 | 数量 |
|---|---:|
| stale | 0 |
| superseded | 0 |
| revoked | 0 |
| archived | 0 |
| invalidated | 0 |

**[观察性证据]** 这并不能证明生命周期机制永远无用，但它证明在当前实验协议和样本中，这些核心状态没有被自然激活。若论文强调它们，必须增加真正允许跨 session、跨尝试或跨版本记忆存在的实验。

### 8.3 15 个 contradiction 的人工语义复核

15 个 contradiction 全部来自 conflicting_structured_value。逐条阅读后：

- 14/15 更像兼容性转述，而非互斥事实；
- 1/15 是实质性污染。

常见误报示例：

- “expense logs list” 与 “expense logs”；
- “VLC tutorial overlay” 与 “tutorial screen”；
- “details of kayaking activity” 与 “activity details page”；
- 对同一 partial expense image 的不同描述。

唯一较实质的案例是：

- 记忆写入“预计会打开新消息编辑页面”；
- 实际观察仍是 SMS 主页面且没有会话。

这个问题不是旧记忆变质，而是**创建时来源污染**：系统把期望结果写成了已经观察到的事实。

按这次人工复核的粗略口径：

- detector flag rate：15/876 ≈ 1.71%；
- 实质性错误冲突：约 1/876 ≈ 0.11%；
- contradiction flags 中约 93.3% 是过敏或假冲突。

必须加上限制：

- 这是非盲法、单人语义审查；
- 没有第二标注者一致性；
- 统计单位是 write event，不是决策机会；
- 不能据此宣布真实错误率就是 0.11%。

但它足以提出一个必须验证的风险：

> 当前 contradiction detector 可能把表述差异当事实冲突，产生大量不必要的降级与验证。

### 8.4 目前没有看到“冲突检测挽救成功”的直接证据

**[观察性证据]** 在 frozen Hard 中 M0 为 0/19。现有材料没有建立如下因果链：

> 自然错误记忆出现 → RAVEN-M 正确识别 → 阻止了本会失败的动作 → 改用正确策略 → evaluator 成功。

未来实验应专门统计这种“prevented harmful action and recovered success”事件，而不能只统计 route 或 guard 被触发多少次。

---

## 9. r61–r78 的 H17 连续诊断到底在做什么

另一个长任务目前没有跑到 r80 或 r90。仓库最新提交为：

> 0327603，2026-08-02 14:00:54 +0800，test(v2.2): freeze r78 H17 repair stop

没有 r79。此前相关 Codex 任务当前为 systemError，不是在后台继续运行。

### 9.1 这些 revision 的真实性质

r61–r78 长期围绕 H17 SportsTrackerActivitiesOnDate 调整：

- 日期定位；
- 两个目标行的精确点击；
- 行身份绑定；
- 进入详情并提取 activity type；
- 返回列表；
- 同 episode 截图证据的 provenance；
- visual critic 是否承认这些截图；
- terminal answer 的来源字段；
- prompt context cap；
- 修复动作的坐标约束。

**[研究推断]** 这主要是 controller、guard、provenance contract 和 prompt engineering 的联合调试，而不是在检验“自然记忆会不会变旧”。

### 9.2 r76

本地记录：

> reports/protocol_v2_2_r76_h17_candidate_stopped.json

结果：

- success = false；
- 16 个执行动作；
- 31 次 model calls；
- wall time 1,233.745 秒；
- prompt tokens 198,891；
- 最大 prompt 9,889，超过 8,192 cap；
- 两个目标行都被访问；
- 两张目标字段图像都被捕获；
- 人工复核可读出 cycling 与 inline skating；
- answer candidate 为 cycling, inline skating；
- 但 executor 把 controller visit key 当成 memory ID；
- visual critic 又把同 episode 的证据图误认为不可靠的历史截图；
- 最终没有执行 answer。

结论：能力链条已经走到答案附近，但证据权限契约相互冲突，导致“有正确证据却无法完成”。

### 9.3 r77

本地记录：

> reports/protocol_v2_2_r77_h17_candidate_stopped.json

结果：

- success = true；
- evaluator reward = 1.0；
- 13 个动作；
- 23 次 model calls；
- wall time 560.688 秒；
- 同 episode routed evidence 被 critic 接受；
- answer action 成功执行；
- 没有 controller 硬编码答案，也没有绕过 critic；
- 但 terminal executor prompt 为 8,982 tokens，仍超过 8,192 cap；
- 因协议 context-cap 要求，整轮被标为 stopped，而不是正式 Gate F 成功。

这证明：

> 同 episode 的两个详情证据可以被正确绑定并支持最终回答。

它没有证明：

- 对其他任务也有效；
- 完整 memory lifecycle 必不可少；
- 比简单结构化槽位更好；
- 在等 token / 等调用预算下仍有优势。

### 9.4 r78

本地记录：

> reports/protocol_v2_2_r78_h17_candidate_stopped.json

结果：

- success = false，reward = 0；
- 7 个执行动作，13 次调用，wall time 610.339 秒；
- 已到目标日期，识别到两个目标行；
- 两行中心分别为 0.747292 和 0.834375；
- 模型先提出 swipe，被 guard 正确阻止；
- repair 改为点击 y=0.785，坐标落在两行之间，无法唯一绑定身份；
- 因而点击被拒绝；
- terminal evidence projection 根本没有走到；
- 更早的 executor repair prompt 为 8,904 tokens，仍超过 cap。

### 9.5 r77 成功、r78 失败说明什么

最重要的不是“r77 曾经成功”，而是：

1. 同一任务在相邻 revision、相同 seed 下仍不稳定；
2. guard 能正确识别危险动作，但修复机制不一定能产生更好动作；
3. 增加精细契约后，prompt 可能膨胀到超出上限；
4. 当前系统成功依赖复杂的任务路径、坐标、证据顺序和提示契约；
5. 需要在完全未参与调试的任务上冻结测试，才能区分通用原则与 H17 特定工程。

### 9.6 是否属于“偷偷调参”

每次修改都有 commit、报告、冻结记录，这比隐瞒修改要规范得多。但科研上的问题仍然存在：

> 连续观察同一 H17 的失败，再依据该失败修改 controller，即使每条规则表面写得通用，也会逐渐把 H17 的结构编码进系统。

因此应把 r61–r78 定义为 development set 上的系统开发，而不能把 H17 再当作无偏测试证据。接下来必须：

- 冻结 controller；
- 将 H17 排除出主测试集，或只作为开发案例；
- 在未见过的 App、任务模板和 seed 上测试；
- 预先注册允许修改的范围；
- 一旦看过测试失败，不再用同一测试集汇报最终泛化。

---

## 10. 当前 claim–evidence 对照表

| 潜在论文主张 | 现有证据 | 当前判断 | 还缺什么 |
|---|---|---|---|
| GUI 长任务需要记忆 | MemGUI-Bench 强消融；多个 GUI agent 工作 | 较强支持 | 在本项目实现中复现 |
| AndroidWorld 中自然记忆错误很常见 | 当前自然日志中实质冲突很少；生命周期事件为 0 | 不支持 | 决策级人工标注与更多任务 |
| FACT/HYPOTHESIS 路由提高成功率 | r39/r56 M0 均弱于 B3 | 当前反证较强 | 公平配对、多 seed、消融 |
| guard 能阻止不合法动作 | Contacts、H17 有多个局部案例 | 支持局部能力 | 证明阻止后能恢复成功 |
| completion gate 减少假完成 | 有拒绝案例 | 部分支持 | 同时报告假阳性和假阴性 |
| contradiction detector 有效 | 20 个注入 corruption fixtures 全部挡住 | 支持受控鲁棒性 | 自然 precision / recall |
| 生命周期管理有现实必要性 | 当前协议无跨 episode 记忆，相关事件为 0 | 不支持 | 跨 session / 跨版本任务 |
| 同 episode 证据路由可完成复杂信息检索 | r77 H17 reward 1.0 | 单案例支持 | 未见任务与简化基线 |
| 完整框架净收益为正 | M0 成本更高且成功更低 | 当前不支持 | 成功—成本联合评价 |
| 方法能域外泛化 | 尚无冻结 OOD 结果 | 不支持 | unseen app/template/seed |

这张表应成为后续论文写作的底线：任何摘要或引言主张都不能超过其证据等级。

---

## 11. 更接近真实问题的失败分类

建议把后续日志按“发生在哪个环节”而不是按“最后是什么 route”标注。

### 11.1 感知层

- 控件漏检：没看到 +；
- 符号识别错误：把 + 看成 ×；
- 语义错误：看到 +，却理解成删除；
- OCR 读错文本或数值；
- 多 patch / 局部信息整合错误。

### 11.2 grounding 层

- 知道要点某控件，但坐标点偏；
- 两个相邻行无法唯一绑定；
- 滑动方向或距离错误；
- 控件身份与点击区域不一致。

### 11.3 动作结果层

- 点击没有产生预期变化；
- App 未响应或网络失败；
- 动作执行成功但页面加载尚未完成；
- 当前状态与模型以为的状态不同。

### 11.4 记忆创建层

- 把期望结果当成观察事实；
- 从当前图像中抽取错值；
- value 正确但 entity / field 绑定错误；
- 把推断写成 observed；
- provenance 丢失。

### 11.5 记忆保持与检索层

- 后续忘记某个已读字段；
- 相似值覆盖；
- 只召回一部分；
- 找到信息却忘记仍未完成的目标；
- 调用到错误任务或错误页面的经验。

### 11.6 计划层

- 自行增加任务未要求字段；
- 忘记保存；
- 忘记跨 App 写入；
- 达到局部目标后误以为任务完成；
- critic 指出问题后仍重复原动作。

### 11.7 完成判断层

- 只看页面跳转就宣布任务完成；
- 没有检查列表/详情/底层 evaluator；
- 正确结果被过严 gate 拒绝；
- 错误结果被过松 checker 接受。

只有第 11.4–11.5 的一部分是狭义“记忆可靠性”。如果实验失败主要在 11.1–11.3 或 11.6，那么给记忆加更多生命周期状态不会解决根因。

---

## 12. 建议的研究方向重构

### 12.1 主线 A：episode 内证据化状态跟踪

建议把 AndroidWorld 主线收缩为：

> **Evidence-grounded episodic state tracking for long-horizon GUI agents**

核心不是怀疑每条历史，而是确保 agent 在长流程中：

- 保存真正任务相关的值；
- 保持值与实体、字段、页面的绑定；
- 记得哪些子目标尚未完成；
- 最终答案或不可逆动作能追溯到证据；
- 不让大量无关历史挤占当前决策上下文。

这个方向与 H17、Expense、多笔记合并、跨 App 写入等真实任务更匹配。

### 12.2 主线 B：风险自适应验证

验证不应由“是否来自记忆”单独触发，而应由错误代价与证据风险共同决定。

可用概念公式表达：

> 当预期错误损失 × 当前出错概率 > 验证成本时，才触发额外验证。

实际无需精确估值，可按风险级别规则化：

| 动作类型 | 允许证据 | 是否额外验证 |
|---|---|---|
| 可逆导航 | 当前截图、作用域匹配的程序提示 | 通常不需要 |
| 探索性点击 | hypothesis 可参与 | 失败后观察即可 |
| 可逆文本编辑 | 任务原文 + 当前字段身份 | 关键字段可检查 |
| 保存/发送/删除 | 目标身份、关键字段、当前状态 | 必须验证 |
| 最终 answer / done | 每个关键结论的证据闭环 | 必须验证 |

触发验证的信号可包括：

- 来源是模型推断而非直接观察；
- 同一实体存在多个不一致值；
- 页面签名或 App 版本与程序记忆不匹配；
- 证据绑定到相似但不同的行；
- 当前动作不可逆；
- 任务即将结束；
- 最近动作多次无页面变化；
- 记忆距离当前决策很远，且中间发生了可能改变状态的操作。

### 12.3 支线 C：长期记忆生命周期

stale、superseded、revoked 不应被删除，但建议暂时降为独立扩展：

- 在 MemGUI-Bench 的 pass@k 跨尝试协议中测试；
- 或自行构造 App v1 → v2 / 首次启动 → 非首次启动的版本化任务；
- 明确区分“旧知识错误”与“旧知识只在另一个上下文仍有效”；
- supersede 表示出现了作用域相同且证据更强的新方法；
- stale 表示超出时间/版本适用范围，仍可保留供审计；
- suppress 表示当前决策不应调用，不等于永久错误；
- 单次失败只能记为失败或不确定，不能立刻推翻旧知识。

---

## 13. 建议的最小新架构

与继续堆叠规则相比，先验证一个更小、因果关系清楚的架构。

### 13.1 五个必要模块

1. **Event Log**
   - 系统级记录动作、截图哈希、UI tree 和结果；
   - 不由 LLM 判断是否发生。

2. **Task-Critical Slot Store**
   - 只保存当前任务需要的实体—字段—值；
   - 每条槽位绑定来源图像、step 和页面身份。

3. **Goal Ledger**
   - 把任务拆成明确 requirements；
   - 标记 open、acquired、executed、verified；
   - 防止找到部分信息后忘记剩余步骤。

4. **Scoped Procedure Cache**
   - 保存局部导航关系；
   - 作用域匹配时直接使用；
   - 不匹配时降级为探索提示。

5. **Risk-Gated Verifier**
   - 只在保存、发送、删除、最终 answer / done 或高歧义实体绑定时调用；
   - 不对每个局部页面转换重复审查。

### 13.2 暂时移出主路径的机制

在未证明自然需求前，以下机制不宜每一步都进入 prompt：

- 完整长期生命周期状态；
- 全量历史自然语言摘要；
- 对所有记忆统一 FACT / HYPOTHESIS 路由；
- 低风险导航上的 critic；
- 多个语义重叠的 guard；
- 与当前决策无关的通用指导文本。

这些机制可以保留在代码或支线实验中，但主实验必须通过消融证明它们各自有增益。

---

## 14. 决定性实验计划

### 14.1 Phase 0：先回答“问题到底多常见”

这是最优先、成本最低、最能决定方向的一步。

#### 标注单位

不要以 route count 为单位，而要以 **decision step / memory use opportunity** 为单位。

每个机会标注：

- 是否需要历史信息；
- 所需信息是否已经观察到；
- 记忆是否创建正确；
- 是否正确保持；
- 是否检索到；
- 是否绑定到正确实体/字段；
- 是否影响动作或最终答案；
- 是否有验证机会；
- RAVEN-M 是否正确介入；
- 介入是否改变最终结果。

#### 错误标签

- creation error；
- retention loss；
- stale applicability；
- entity/value mis-association；
- goal drift；
- no-effect action memory；
- false completion；
- false contradiction；
- unnecessary verification。

#### 标注质量

- 最好由两名标注者独立标注；
- 先制定 20 个例子的判定手册；
- 报告一致率或 Cohen’s kappa；
- 对争议案例做合议；
- 标注者尽量不知道轨迹来自 B3 还是 M0。

#### 预注册停止规则

例如：

- 若自然 harmful-memory opportunity < 2%，则不再声称 AndroidWorld 的常规成功率主要受可靠性生命周期限制；
- 若 false contradiction precision < 50%，先修 detector，不进入大规模实验；
- 若 guard 阻止错误后恢复成功的比例接近 0，则研究重点转到 recovery policy，而不是继续加阻止规则。

阈值可以调整，但必须在看完整结果前确定。

### 14.2 Phase 1：AndroidWorld episode-memory 小规模试验

#### 任务选择原则

只选真正需要在多个页面间保持信息的任务；另加不需要记忆的负对照。

候选 memory-heavy：

- H02 ExpenseAddMultipleFromGallery；
- H03 ExpenseAddMultipleFromMarkor；
- H06 MarkorMergeNotes；
- H07 MarkorTranscribeVideo；
- H10 / H11 / H12 recipe 类任务；
- H15 receipt / expense 类任务；
- H17–H19 多条目查询与回答。

负对照：

- Contacts 单条创建；
- 其他短路径、当前页面可直接完成的任务。

Contacts 不应被当作“记忆可靠性提高成功”的主要正例，因为其关键难点更接近表单填写与完成检查。

#### 最小对比组

| 变体 | 目的 |
|---|---|
| B0 Current Screen Only | 测量完全无历史的下界 |
| B3 Simple Summary | 强而简单的历史基线 |
| M-SLOTS | 结构化实体—字段槽位 + goal ledger，无复杂生命周期 |
| M-RISK | M-SLOTS + 仅高风险位置 verifier |
| Full RAVEN-M | 当前完整方法 |

如果预算紧，第一阶段先跑 B3、M-SLOTS、M-RISK；只有 M-RISK 有信号时再加入 Full。

#### 公平性

- 同模型；
- 同任务实例；
- 同 seed；
- 同 screenshot / UI tree；
- 同 action budget；
- 同最大 model call；
- 同 prompt token cap；
- 同允许动作集合；
- 同 evaluator；
- 成对执行；
- 顺序随机化；
- 报告基础设施失败并从方法失败中分离。

必须额外加入：

- **token-matched baseline**：避免完整方法仅因拥有更多调用而占优；
- **call-matched baseline**：避免 critic 数量不同造成不公平；
- **固定 controller 冻结**：测试集上不继续按失败改规则。

#### 主要指标

1. Task Success Rate；
2. Information Acquisition Accuracy；
3. Retention Rate；
4. Entity–Field Association Accuracy；
5. Goal Completion Recall；
6. Completion false-positive / false-negative；
7. Verification precision / recall；
8. Prevented harmful actions；
9. Unnecessary verifications；
10. Actions、calls、prompt tokens、completion tokens、wall time；
11. Success per million tokens；
12. 成功条件下的中位耗时。

#### 核心假设

H1：

> 在 AndroidWorld 的 memory-heavy 子集上，M-SLOTS 相比 B3 能在相同预算下提高实体—字段保持和最终成功率。

H2：

> 在自然高风险机会存在时，M-RISK 相比 M-SLOTS 能减少错误保存/错误回答，同时不显著增加低风险导航成本。

H3：

> Full RAVEN-M 只有在其 lifecycle / routing 组件提供独立增益时才值得保留；否则应以更小方法为主。

#### 分阶段规模

Pilot：

- 6 个 memory-heavy 任务 + 2 个负对照；
- 3 个变体；
- 每个任务 2 个 seed；
- 共 48 个 cells。

只有出现方向一致的信号后再扩展：

- 12 个任务；
- 4–5 个变体；
- 每任务 5 个 seed；
- 240–300 个 cells。

不要在没有自然错误机会的情况下直接烧大量 GPU。

### 14.3 Phase 2：MemGUI-Bench

目的：正式测试跨页面、跨 App、跨尝试记忆。

建议：

- 先使用官方 quick subset 或选择约 40 个任务做系统兼容；
- 再扩展到完整 128 任务；
- pass@1 评估同 episode 短期记忆；
- pass@3 或更多 attempts 评估长期经验积累；
- 保持 agent state 与清空 state 两种设置；
- 报告 IRR / FRR 等论文指标；
- 将任务按 PMH、ProcMH、OMH 机会分层。

这里才适合验证：

- 尝试 1 的经验是否帮助尝试 2；
- 错误经验是否会污染后续；
- supersede / suppress 是否有实际用处；
- 长期记忆带来的增益是否覆盖时间成本。

### 14.4 Phase 3：受控 corruption robustness

这一阶段作为独立鲁棒性实验，不与自然成功率混写。

预先定义污染类型：

1. 精确值篡改；
2. entity–value 互换；
3. 页面相似但作用域错误；
4. 把 expected outcome 写成 observed；
5. 兼容性转述；
6. App 版本变化后的过时 procedure；
7. 第一次启动专属步骤被错误用于后续启动。

按污染率绘制 robustness curve：

- 0%、5%、10%、20%、40%；
- 比较 B3、M-SLOTS、M-RISK、Full；
- 报告成功率、错误记忆使用率、误拒率和额外成本。

关键是同时加入“兼容性转述”，否则 detector 只会在明显错误上看起来很强，却不暴露假冲突。

### 14.5 Phase 4：域外泛化

至少有三种 holdout：

- unseen tasks：同 App 未见任务模板；
- unseen apps：开发时从未使用的 App；
- unseen seeds / instances：同模板新参数。

理想设置：

- r1–r78 和已检查任务全部视为 development；
- 在冻结 commit 上一次性运行 holdout；
- holdout 结果出来后，不再修改并重跑同一 test；
- 若必须继续开发，旧 test 自动降级为 development，另建新 holdout。

---

## 15. 统计分析与报告方式

### 15.1 成功率

- 同一任务、seed 的二元成功可用 McNemar test；
- 同时报 paired bootstrap confidence interval；
- 不只报总平均，也按任务类型分层。

### 15.2 效率

- actions、calls、tokens、wall time 多半偏态；
- 报中位数、四分位距和 paired bootstrap；
- 不要只报平均数；
- 将基础设施等待时间与 agent 推理时间分开。

### 15.3 记忆质量

至少报告：

- creation precision；
- retrieval recall；
- entity-binding accuracy；
- contradiction precision；
- stale detection precision / recall；
- harmful-memory interception rate；
- recovery-after-interception rate。

其中最关键的新指标是：

> **拦截后恢复成功率**，而不是“拦截次数”。

### 15.4 净效用

可以定义一个透明的辅助指标：

> Utility = Task Success − λ1 × normalized calls − λ2 × normalized tokens − λ3 × unnecessary blocks

但主表仍应保留原始指标，不能用自定义加权分数掩盖真实成本。λ 必须预先设定并做敏感性分析。

---

## 16. 可能结果与对应决策

### 情况 A：M-SLOTS 显著好于 B3，M-RISK 继续提升

结论：

- 结构化事实保持是真实贡献；
- 风险自适应验证也有独立价值；
- 可以把 RAVEN-M 重构为更轻的 evidence-grounded episodic memory。

### 情况 B：M-SLOTS 好于 B3，但 M-RISK / Full 无额外收益

结论：

- 问题主要是信息组织与目标跟踪；
- 复杂可靠性生命周期应删减或降为可选扩展；
- 论文不要把贡献写成 universal reliable memory governance。

### 情况 C：只有人工污染下 Full 有优势

结论：

- 方法是 corruption robustness / safety 方法；
- 应诚实改写任务设定；
- 需要说明污染模型是否现实；
- 不能声称提高标准 AndroidWorld 常规能力。

### 情况 D：所有记忆变体都不如简单摘要

结论：

- 当前基准、模型或实现阶段不支持该研究方向；
- 先修基础 controller，或转到 MemGUI-Bench；
- 不应继续通过增加规则挽救同一主张。

### 情况 E：H17 有增益，但 unseen tasks 没有

结论：

- 存在开发集过拟合；
- r61–r78 只能作为工程案例；
- 方法需要重新抽象，不能用 H17 作主结果。

---

## 17. 对夏令营任务与论文潜力的分开评价

### 17.1 作为夏令营考核

当前项目已经体现出较高完成度：

- 跑通了 AndroidWorld 与自建协议；
- 有可追溯 commit 和 revision；
- 有 baseline、完整方法、冻结报告；
- 有 critic、guard、memory route、completion verification；
- 对失败进行了细粒度 forensic analysis；
- 没有隐瞒无效或停止的实验。

对于“能否搭建并理解一个复杂 baseline”而言，这些远超只跑出一个结果。

### 17.2 作为可发表研究

距离论文级证据仍有明显差距：

- 主问题在 benchmark 中的自然发生率未知；
- 主要 baseline 结果目前不占优；
- 成本显著更高；
- 方法组件过多，缺少清晰消融；
- controller 经过同一任务长期调试；
- 尚无域外泛化；
- 长期生命周期与 episode-local 协议不匹配；
- robustness、inspectability 和 capability 的主张混在一起。

真正能让老师看到科研潜力的，不是继续把 r78 修到 r100，而是指出：

> 原假设哪里没有被实验支持，为什么不支持，怎样用最小决定性实验推翻或保留它，以及如何把方法重构到更真实的问题上。

这比单纯“把任务跑通”更接近科研能力。

---

## 18. 给 GPT Pro 的独立审查任务

下面这段可以连同全文直接交给 GPT Pro：

### 审查角色

你是一名对 GUI agent、multimodal agent、memory-augmented agent 和 benchmark methodology 熟悉的严格审稿人。不要默认 RAVEN-M 值得保留，也不要为了迎合作者而补充包装。请依据本文中列出的本地实验、协议事实和外部论文，对研究问题本身进行独立审计。

### 必须回答的问题

1. 请把 RAVEN-M 的主张拆成最小可证伪命题，并判断每条命题当前是 supported、partially supported、unsupported 还是 contradicted。
2. AndroidWorld 的 episode reset 与 RAVEN-M 的 episode-local memory 是否使 stale / superseded / revoked 主张失去可评测性？若是，最小修正是什么？
3. “点击 + 后进入新建联系人页”这类短时局部事实，是否应当被高信任？请提出一个风险—作用域联合的形式化规则，避免全局不信任。
4. 当前自然日志中的 contradiction 误报、lifecycle 零事件和 M0 失败，是否足以要求 pivot？还需要什么最小证据才能作决定？
5. r61–r78 连续围绕 H17 的修改应怎样定义：合理开发、任务特定调参，还是 benchmark overfitting？最终测试协议应怎样隔离它？
6. r77 的 reward 1.0 究竟证明了哪个最小能力？哪些更大主张绝对不能由它推出？
7. 请比较以下三个研究故事：
   - 可靠记忆生命周期；
   - episode 内证据化状态跟踪；
   - 风险自适应完成验证。
   哪一个最有真实问题、方法必要性和可验证性？
8. 哪些现有模块应保留、合并、移出主路径或删除？请给出最小架构，而不是继续堆规则。
9. 请设计一个 48-cell 以内、能够决定 GO / PIVOT / STOP 的 pilot。要求同模型、同 seed、同预算、强 baseline、预注册失败判据。
10. 请指出本文实验计划中仍可能存在的 confound、数据泄露、适应性过拟合和评价偏差。
11. 如果目标只是夏令营高完成度，当前成果是否足够？如果目标是论文，最短的可信路径是什么？
12. 最终必须给出一个明确决策：
    - GO：保持主假设；
    - REVISE：保留核心但缩小主张；
    - PIVOT：转向新的主问题；
    - STOP：当前方向不值得继续投入。

### 输出格式

请按以下顺序输出：

1. 一页执行摘要；
2. claim–evidence 表；
3. 最强支持证据；
4. 最强反证；
5. 方法最小化建议；
6. 决定性 pilot 设计；
7. 论文故事线；
8. 三名不同立场的审稿意见；
9. GO / REVISE / PIVOT / STOP 决策及理由；
10. 如果信息不足，列出需要读取的具体本地文件或日志字段，不要凭空补事实。

### 审查原则

- 不把设计精细等同于效果有效；
- 不把一次成功等同于泛化；
- 不把拦截错误等同于恢复成功；
- 不把注入污染鲁棒性等同于自然任务收益；
- 不把 route 数量等同于真实 hazard 数量；
- 不把更高成本下的结果与低成本 baseline 直接比较；
- 不因为项目已投入大量时间就降低证据标准。

---

## 19. 建议 GPT Pro 优先读取的本地材料

### 协议与系统

1. RAVEN-M-Research\reports\method_and_system.md
2. RAVEN-M-Research\04_protocols\experiment_protocol.md
3. RAVEN-M-Research\05_project\src\raven_m\memory\models.py
4. AndroidWorld episode runner：https://github.com/google-research/android_world/blob/main/android_world/episode_runner.py
5. AndroidWorld M3A：https://github.com/google-research/android_world/blob/main/android_world/agents/m3a.py
6. AndroidWorld suite utils：https://github.com/google-research/android_world/blob/main/android_world/suite_utils.py

### 关键实验报告

1. RAVEN-M-Research\reports\protocol_v2_2_r39_gate_e_final.md
2. RAVEN-M-Research\reports\protocol_v2_2_r56_gate_e_final.md
3. RAVEN-M-Research\reports\breadth_forensic_analysis_2026-07-26.md
4. RAVEN-M-Research\reports\g6_inspectable_memory.md
5. RAVEN-M-Research\reports\protocol_v2_2_r76_h17_candidate_stopped.json
6. RAVEN-M-Research\reports\protocol_v2_2_r77_h17_candidate_stopped.json
7. RAVEN-M-Research\reports\protocol_v2_2_r78_h17_candidate_stopped.json

### 文献

1. AndroidWorld：https://arxiv.org/abs/2405.14573
2. MemGUI-Bench：https://arxiv.org/abs/2602.06075
3. MemGUI-Bench repo：https://github.com/lgy0404/MemGUI-Bench
4. HAR-GUI：https://arxiv.org/abs/2511.09127
5. PG-Agent：https://doi.org/10.1145/3746027.3755189；代码：https://github.com/chenwz-123/PG-Agent
6. AppAgent：https://arxiv.org/abs/2312.13771
7. Mobile-Agent-E：https://arxiv.org/abs/2501.11733

---

## 20. 后续工作清单

### 立即做

- [ ] 冻结 r78 及此前所有已观察任务为 development evidence；
- [ ] 对自然 memory hazard 做 decision-level 双人标注；
- [ ] 计算 contradiction detector 的自然 precision；
- [ ] 统计 guard 拦截后真正恢复成功的比例；
- [ ] 将完成门控的 false positive / false negative 分开；
- [ ] 确定 AndroidWorld 主线是否改为 evidence-grounded episodic state；
- [ ] 搭建 B3、M-SLOTS、M-RISK 三个最小可比变体。

### 有信号后再做

- [ ] 扩展到多 seed；
- [ ] 加 token-matched / call-matched baseline；
- [ ] 在 unseen task / unseen app 上冻结测试；
- [ ] 接入 MemGUI-Bench；
- [ ] 做跨尝试 long-term memory；
- [ ] 做预注册 corruption robustness curve。

### 暂时不要做

- [ ] 不要继续只围绕 H17 增加新规则；
- [ ] 不要仅因 r77 成功就启动大规模正式实验；
- [ ] 不要把 20 个 injected fixtures 写成自然有效性证据；
- [ ] 不要在主结果中混合 formal、development、archived retry 统计；
- [ ] 不要让 GPT Pro 在没有读取原始报告时替项目补出“看起来合理”的结论；
- [ ] 不要在未证明自然需求前让所有生命周期信息进入每一步 prompt。

---

## 21. 审稿人式自检

### 21.1 Contribution

- 当前真正的新知识是什么：可靠性生命周期、证据权限，还是风险自适应验证？
- 目标失败是否在真实任务中足够常见？
- 方法是否超出普通结构化 memory + verifier 的显然组合？
- 最有价值的贡献会不会其实是“可靠性框架可能产生负净收益”这一诊断发现？

当前状态：**needs revision / needs new experiment**

### 21.2 Writing clarity

- 各类 memory 是否有互斥定义？
- FACT、HYPOTHESIS、ALERT、SUPPRESS 是否对应明确行动权限？
- observed、expected、inferred 是否严格区分？
- completion evidence 是否独立于普通 memory？

当前状态：**needs revision**

### 21.3 Experimental strength

- 是否优于强 baseline？
- 是否有多个 seed？
- 是否报告成本？
- 是否存在 unseen 泛化？

当前状态：**不通过；现有 B3 优于 M0，缺少 OOD**

### 21.4 Evaluation completeness

- 每个模块是否有消融？
- 是否有自然 hazard incidence？
- 是否有 detector precision / recall？
- 是否有 recovery-after-block？
- 是否有 matched-budget baseline？

当前状态：**needs new experiment**

### 21.5 Method soundness

- 是否在现实协议中需要长期生命周期？
- guard 是否会制造死锁？
- prompt 是否超过上下文 cap？
- 规则是否因 H17 连续调试而过拟合？
- 净收益是否为负？

当前状态：**高风险，需要先做最小化与决定性 pilot**

---

## 22. 最终建议

如果现在必须做一个研究方向决策，本文倾向：

> **PIVOT，但不是放弃整个项目。保留证据化记忆、任务关键槽位、目标账本和高风险完成验证；把“普遍的记忆不可信与生命周期治理”从 AndroidWorld 主线降为跨 session 的独立支线。**

最短可信路径是：

1. 用现有日志定量证明自然问题是什么；
2. 用最小 M-SLOTS 验证结构化事实保持；
3. 用 M-RISK 验证只在高风险处检查是否有净收益；
4. 在 unseen tasks 上冻结测试；
5. 若要保留 stale / superseded，再转到 MemGUI-Bench 或版本化环境。

这条路径不否定前面的 r1–r78。相反，它把这些 revision 从“必须证明完整方法有效”转化为一组很有价值的系统诊断材料：它们清楚展示了一个可信 GUI agent 系统中，感知、grounding、历史、证据权限、critic、guard、上下文和完成判断如何相互作用，也展示了安全约束如何在没有恢复策略时变成新的失败源。

真正值得继续追问的研究问题不是：

> 怎样写更多规则，让系统永远不相信记忆？

而是：

> **在什么决策上，什么来源的历史信息值得被信任到什么程度；怎样用最小成本验证高风险结论，并让验证失败后仍能恢复任务。**

这个问题更贴近真实 GUI agent，也更容易被严谨实验支持或推翻。
