# SYS-TRC Request: Triggered Recovery Critic

Design one prospective system testing whether a critic helps only after a
deterministic visible-stagnation trigger. Recurrence detection is a trigger, not
the claimed intelligence. Do not merge other SYS tracks.

Allowed: a task-agnostic RGB/action stagnation detector; one same-model critic
call per mature trigger; a bounded output containing exactly a failure
hypothesis and one recovery principle; one-time injection into the next executor
call. Freeze a strict auxiliary budget, no more than four critic calls per
episode and preferably lower from trace evidence.

Excluded: opening plan, stepwise verifier, action block/override, donor bank,
long-lived free-text memory, evaluator/hidden UI/future frame, task/app rules,
or reflection chains. Generic "try another approach" is not a mechanism.

Freeze recurrence/no-progress equivalence, maturity, prompt/schema, advice
validation, single-use/expiry/cooldown, budget, and failure handling. Compare
Full to `CRITIC-SHADOW` with identical triggers/calls and to a generic active
extra-reasoning control. A productive critique must cause the first post-critic
action to diverge, escape within four steps, and avoid relapse for four steps.
The component outcome signal also requires Full to beat its control in paired
success, not merely emit text.

Use the common 4/4 -> Recipe -> remaining-14 schedule and independent accuracy,
cost, and causal verdicts. Count executor and critic resources separately.

Return only
`GPT_PRO_SYS_TRC_TRIGGERED_RECOVERY_CRITIC_DESIGN_2026-08-13.md`, with one frozen
design, exact algorithms/prompts/caps, implementation boundary, replay/preflight,
controls, live gates, verdict schema, and decisive stop rules. No code or GPU.
