# INFRA-M8 freeze record

Audit commit: `960a81817ebdcc6de5bec8b306778a6b077ce8fc`.

M8 separates the complete process observation universe from project authorization candidates. Full-snapshot identity supplies existence, currentness, PID reuse and ancestry; it grants no role. The filtered, hash/listener-enriched view supplies role authority. Missing, duplicate, truncated or cross-view-inconsistent evidence fails closed.

Property/fault coverage includes: valid parent only in the full snapshot; missing parent; runner PID reuse/creation mismatch; unrelated parent; truncated universe; candidate/universe mismatch; child hash/listener/port rejection; stale-history parent replacement; continuing-client real-currentness; PID ambiguity; and full trigger plus derived-view persistence.

Generation and held-out budgets are zero. One DEV live chain is permitted only after all offline gates pass and the lock/tag are created. First live failure stops; 12/12 alone may authorize later v0.3 preparation.

## Offline gate result

All gates passed. Focused counts: M8 19/19, M7 29/29, M6 22/22, M5 15/15 and M4 terminal accounting 17/17. The complete role-binding-timing namespace passed 245 tests. Full regression contained exactly the pre-existing frozen-r79 manifest conflict and no additional failed node. Schema corruptions, two-view source isolation, M8 audit linkage, protected/binary/psutil hashes, base-config hash and zero-mutation runtime preflight passed; no controlled listener, project runtime process, live output root or M8 temporary residue existed.
