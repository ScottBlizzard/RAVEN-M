# INFRA-M7 policy-freeze record

This record freezes only the runner-owned ADB-client authority revision and its one DEV maintenance qualification. The preceding audit is commit `d9276246402e23b2347bb69777d2a8782b58e895`.

- M6 remains an immutable false rejection at `FRAMEWORK_RUNTIME:EXCLUDED_PID_DRIFT`.
- M7 replaces harmless-subcommand enumeration with a structural direct-runner client predicate.
- Exact official ADB path/hash, global `-P 5038` prefix, direct current runner parent, creation order, 45-second active bound, zero listener ownership and PID/argv continuity are conjunctive.
- Server lifecycle remains separately phase-authorized; wrappers, 5037, missing evidence and unowned clients fail closed.
- Full trigger snapshots and 250 ms listener-bearing process history are mandatory.
- Generation calls = 0; held-out captures = 0; all touched scenes are DEV-contaminated.
- One frozen live chain only; first failure stops; no automatic v0.3 execution.

The machine lock and freeze tag are created only after all offline gates pass.

## Offline-gate record

Attempt 1 is preserved as `INCOMPLETE_NOT_PASS_OR_FAIL`: its subprocesses completed, but the summarizer used the nonexistent audit key `unique_invocations` and raised before producing a terminal gate verdict. No output was discarded.

Attempt 2 passed. Focused counts were M7 29/29, M6 22/22, M5 15/15, and M4 terminal accounting 17/17; the full role-binding-timing namespace passed. Full regression reported exactly the pre-existing `test_r78_candidate_static_manifest_validation_passes` frozen-r79 manifest conflict and no other failed node. Schema corruptions, source boundaries, the six-invocation M6 audit, protected hashes, binary hashes and zero-mutation runtime preflight passed; no controlled listener, project runtime process or M7 temporary residue was present.
