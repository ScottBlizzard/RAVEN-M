# A1-R8 Route-Cycle Recovery Pending Ledger Preregistration

Parent evidence is commit `721af9efb1cba52cfa3db210a90eb9152e9e3699`. A1-R7 produced a material two-action cycle (open item, wrong detail tap, return, repeat); therefore its stationary failure trigger had zero exposure.

R8 inherits R7 and retains only the last four material-transition canonical action families. Exact `A,B,A,B` with `A != B` creates one one-shot route-cycle record. The next normal prompt reports the two-step recurrence and requires a different second control/coordinate after at most one reopen, with full-screen y calibration and visible completion. State is bounded to four trace items, one pending record, and eight delivered signatures. It adds no model call, action block/override, forced stop, hidden input, whitelist, or budget change. The frozen six-task fail-fast gate and full-suite criteria are unchanged.
