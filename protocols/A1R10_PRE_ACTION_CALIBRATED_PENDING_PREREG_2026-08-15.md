# A1-R10 Pre-Action Calibrated Pending Preregistration

Parent evidence is `741bbc83b469bae34aec7eb44723d26a448b26e0`. A1-R9 detected and rendered three route-cycle recoveries, but late recovery did not change the wrong spatial policy.

A1-R10 is explicitly a minimal composite intervention rather than a pure-memory claim. It inherits R9 and adds the same fixed full-screen normalized-coordinate calibration sentence to every ordinary prompt before the action. It remains prompt-only and adds no model call, action override/block, hidden UI, evaluator, whitelist, forced stop, or step change. Its result must be reported as memory-plus-execution-calibration and cannot be attributed to memory alone. The frozen six-task gate and full-suite criteria remain unchanged.
