# A1-R7 Grounding-Recovery Pending Ledger Preregistration

Parent evidence commit is `bfafa3c3461af400463d172adbe9bfd6aaa5d0af`. R6 preserved the full requirement set and identified the correct Tuition Fees trash control, but missed it spatially and then repeated an ineffective row coordinate despite a weak warning.

R7 inherits R6 and changes only the renderer after two same-family, same-region actions produce no material visible progress for the active ledger. The next read says `RECOVERY REQUIRED FOR THE NEXT ACTION`, forbids repeating that action family/coordinate region, and reminds the controller that tap y is normalized over the full phone screenshot including system bars. It remains prompt-only: no action is blocked, rewritten, or executed by memory; no extra call, hidden UI, evaluator, whitelist, forced stop, or step change is allowed.

The frozen six-task gate, fail-fast scientific stopping, infrastructure rules, remaining-thirteen release, and separate accuracy/cost/causal verdicts are unchanged.
