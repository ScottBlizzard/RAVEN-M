# A1-R9 Run-Length Cycle Recovery Preregistration

Parent evidence is `95cf4e6185769db17504fe3140c00efc9c664e22`. R8's strict ABAB detector had zero exposure because the observed route repeated one family several times between the same wrong second action.

R9 keeps at most eight material-transition action families. It triggers only when the current family equals a prior family two to five retained positions back and every intervening family is one identical, distinct family. This recognizes `B,A+,B` while rejecting ordinary same-family repetition and heterogeneous navigation. It reuses the frozen one-shot R8 recovery renderer and remains prompt-only. All six-task gates and boundaries are unchanged.
