# RAVEN-M / 浙大夏令营课题三完整交接文档

> **2026-08-18 当前最高优先级：SYS-R2-LRER 已完成全部无卡冻结与 PASS preflight，下一步是签 fresh live receipt 后开跑。**
> 这是一条从 R15 成功行为提出、但不继承 R15 EVR/parser 的全新 prospective
> composite identity。法证发现：R15 的 EVR render/read 始终为 0；真正与正确
> `1120` 同时出现的中介，是格式失效后未被 R2 去重丢弃的原始 model-authored
> Thought+Action，普通 history 因而保留了 `1,8,10,7,2`。R2/R13D/R14 到结果输入
> 前已把这些观察从 ordinary history 剥掉，并分别猜成 120/720/2310。
>
> 新系统冻结 R2，不扩展任何 Browser 正则；只在 `executed/max >= 0.70` 后首次
> result action 前延迟一次，把最近 8 个已经执行的原始 Thought+Action 作为瞬时
> evidence block 注入下一次普通 executor 请求，要求按顺序重构、覆盖检查和独立复算。
> 它不增加 auxiliary model call，不自动计算/覆写动作，不读 task/app/UI tree/evaluator/
> reward/future，native action budget不增加。零生成 replay 在固定七题上仅 Browser
> step 18 有一次 opportunity，R2 六个历史成功题全部 0；R15窗口覆盖五值，真实最大
> render `5250 < 5400`。
>
> - implementation commit：`377324739ab86142f1599530968d013f60128b6f`
> - replay content SHA：`01551b79870ba738dc9e139858bb89bcc490720b61aa4fcdf4b609e7d776ef34`
> - source-freeze content SHA：`816b453d5550d0e2c7908195ff9bd366a8b63746e50b71d61db981a5336a4467`
> - preflight：`PASS`，errors=[]，generation_calls=0；content SHA
>   `28ffbdfb93d599ab3a6a08226c74c78afcb3bfd5db367f11017a9268b043891c`
> - focused regression：32/32 PASS；fresh-clone replay snapshots已闭合，不依赖ignored `runs/`
> - 第一次远端启动在任何模型请求前因 FlashInfer sampler 与 Blackwell `sm120` 不兼容退出；
>   已在冻结启动脚本中显式设置 `VLLM_USE_FLASHINFER_SAMPLER=0`，以新 implementation
>   identity 重做 source freeze/preflight。该事件是 retained pre-live infrastructure repair，
>   没有 episode、generation 或科学结果。
> - live顺序：Browser + R2六成功，七题 non-fail-fast；只有7/7才释放剩余12。
>
> 下方 `NO_GO_R15_DERIVED_LIVE_CANDIDATE` 只否定“silent EVR 已被证明有效”和继续
> R16/R17 parser补丁，不否定这条在新identity中前瞻冻结的 raw-evidence hypothesis。

> **2026-08-18 REOPEN：commit `46d9248` 的 NO_LIVE 结论保留为原 Pro confirmatory qualification 审计，不再被当作方向级 exploratory live 的终点。**
> 新协议将 protocol-validity G0 与 confirmatory/risk gate 分开；P1/P2/P3 三个
> exploratory Full treatment 已在任何新 generation 前一次性冻结。每个通过 runtime
> 合法性 G0 的臂必须按 Browser + R2 六成功的固定顺序 NON-FAIL-FAST 跑满七题；仅
> 7/7 才释放剩余十二题。详见
> `protocols/EXPLORATORY_DIRECTION_DIAG_PREREG_2026-08-18.md`。

> **2026-08-18 最新流水线覆盖：R15 法证 + P1/P2/P3 三方向审计全部完成；没有新候选通过 G0。**
> 本节覆盖下方“只允许先做设计/等待用户”的旧指示。固定七题门为 BrowserMultiply、
> R2 六个成功题（最后 OsmAndMarker），所有通过 G0 的候选 NON-FAIL-FAST 跑完七题；
> 只有 7/7 才释放剩余 12 题。有效失败不重跑，silent success 不归因。

## 2026-08-18：R15 Browser 法证与 P1 failure-recovery G0

R15 raw episode 已与 A1、A2、A6、A7、R2、SYS-NAG V4、R13D、R14 做逐 step
CPU-only 法证；A0 本地只有 summary/notes，明确标记 raw missing，不制造对照。R15 在
普通模型 Thought/Action history 中逐步保留 `1,8,10,7,2`，step 17 算出 1120，
step 18 输入 1120，step 19 Submit，step 20 看到 Success。EVR 自始至终 render/read=0。
R2、R13D、R14 到达相同五值序列后分别输入 120、720、2310；所有对照从初始截图
字节起已不相同，不存在 exact-prefix counterfactual。因此裁决为：

- `TARGET_SUCCESS_WITHOUT_MATURE_EVR_READ_UNATTRIBUTED`；
- `NO_GO_R15_DERIVED_LIVE_CANDIDATE`；
- 不做 R15 重跑，不做 R16/R17 parser 扩表达，不与 R2 拼成 7/19。

正式法证：

- `evidence/r15_browser_forensics/R15_BROWSER_FORENSIC_2026-08-18.md`
- `evidence/r15_browser_forensics/R15_BROWSER_FORENSIC_2026-08-18.json`
- JSON content SHA：`3fc94b9219d7b9f5711241c57a01b5e14ba8d130343c1f4eb7ca6e82860fef22`

P1 完整读取原始 Pro failure-recovery 蓝图后，将唯一推荐 TCRA-R2 收敛为其原文冻结
的 E1/E2 detector + 一次同 Qwen 反事实候选 + 确定性 A/B 仲裁。19 条正式 R2 raw
episode 的零生成 G0 结果为：失败覆盖 6 题、4 个任务族，但 R2 成功题
`SimpleCalendarAddOneEvent` 在 step 13/14 产生两个 call-gated E2 event，违反原设计
“六成功 event=0”的硬门。不能在看过 label 后改窗口/route/阈值，因此正式封存：

- `PREFLIGHT_INVALID_NO_LIVE`，不是 0/7；
- `generation_calls=0`；
- 不烧卡、不生成非法 live，直接继续 P2 long-horizon coordination；
- audit：`evidence/p1_failure_recovery/P1_TCRA_R2_ZERO_GENERATION_AUDIT.json`
- audit content SHA：`6023923ffa8deed63e61a1a4117019f9591ba36f20beb939eb6beed43ff86240`
- adjudication：`design_reviews/pro_candidates/2026-08-15/P1_FAILURE_RECOVERY_ADJUDICATION_2026-08-18.md`

P2 完整读取 Pro long-horizon coordination 蓝图后，核验了 R2 19/19 episode 和全部
截图哈希，并物化固定中点暴露：12/19 到达，失败组 9/13、成功组 3/6（Expense、
Calendar、OsmMarker）。但原方案把“双人独立盲审、>=90% step 语义标注、kappa>=.70、
仲裁和 coordination/wrong-track 标签”列为不可跳过的 G0；仓库中这些证据均不存在。
用轨迹长度/RGB proxy 或让 GPT 自己当 reviewer 会实质改变研究构念，不能算最小接口修复。
因此正式封存：

- `PREFLIGHT_INVALID_NO_LIVE`，不是 0/7；
- `generation_calls=0`，未调用 GPU；
- audit：`evidence/p2_long_horizon/P2_SCOPE_R2_ZERO_GENERATION_AUDIT.json`
- audit content SHA：`137c8f76760251b3c8214d4746289c18ad8d398f61cec73cf8305a168033424e`
- adjudication：`design_reviews/pro_candidates/2026-08-15/P2_LONG_HORIZON_COORDINATION_ADJUDICATION_2026-08-18.md`
- 下一步：P3 outcome/completion judgment。

P3 完整读取 Pro outcome/completion judgment 蓝图，并关闭其旧文所述“R2 raw 缺失”问题：
正式 R2 19/19 episode 和截图全部验哈希。精确投影显示 T2 terminal-open-claim 会暴露
R2 六个成功题全部 6/6，T3 refresh proxy 暴露其中 5/6；同时也覆盖若干失败题。这证明
有 opportunity，也证明 preservation 风险极高。原方案要求在 prompt/trigger/live 前冻结
双人 independent visible-only annotation；仓库不存在 reviewer identity、blind labels、
disagreement/adjudication。让同一 Qwen 自证、用 reward/future/UI tree 反推、或偷换成
已做过的 SYS-NAG deterministic guard 均不合法。因此正式封存：

- `PREFLIGHT_INVALID_NO_LIVE`，不是 0/7；
- audit：`evidence/p3_outcome_judgment/P3_SCER_R2_ZERO_GENERATION_AUDIT.json`
- content SHA：`f56f1d43022e353b8623a00ded4e55c241c42a6ac7607b0969e863fc1fa0c8cb`
- adjudication：`design_reviews/pro_candidates/2026-08-15/P3_OUTCOME_JUDGMENT_ADJUDICATION_2026-08-18.md`

四方向总矩阵已封存：

- `evidence/candidate_pipeline/CANDIDATE_PIPELINE_RESULT_2026-08-18.md`
- `evidence/candidate_pipeline/CANDIDATE_PIPELINE_RESULT_2026-08-18.json`
- JSON content SHA：`f158b875018e618ad2d793a4bf9171944dd33b2c59f6f5fb43c7e5bb1671a737`
- 新科学有效 live candidates = 0，因此固定七题需运行数 = 0；没有把 G0 invalid 伪写成 0/7。

验证：本轮 R15/P1/P2/P3/总矩阵 focused tests `13/13` 通过，相关脚本
`py_compile` 与 `git diff --check` 通过。完整 `official_qwen_mobile` 回归仅有 6 个历史
finalizer 测试按设计 fail-closed：A1-R13/R13D/R14/R3-v3 的旧 source-freeze 检测到
当前共享 controller/runner 已不同于各自冻结 implementation commit；没有本轮新测试或
新审计失败。不得为了全绿重签或覆盖这些历史 freeze。

三份用户提供的原始 Pro 文档保存在 `design_reviews/pro_candidates/2026-08-15/`，身份为
`unvalidated candidate blueprint`，不是证据。后续顺序冻结为 P2 long-horizon、P3
outcome/completion；每个方向独立 source-freeze，不能用 P1 具体 task/step 热调参。

> **2026-08-18 最高优先级覆盖：纯记忆解析线在 A1-R15 后关闭。**
> 本节覆盖下方所有较早的“当前状态”和“下一步”指示。不得续跑 A1-R15，
> 不得设计 R16/R17 扩大 Browser 单条 live 表达正则；下一系统必须先由全新
> GPT Pro 在截至本次封存 commit 的证据边界上重新选择并预注册。

## 2026-08-18：A1-R15 target-first 正式结果与停止边界

A1-R15 是从冻结 A1-R2 出发的纯记忆诊断：保留 R2 pending ledger，并用模型
自己已经生成的 Thought 中“明确观察到的单个整数”形成最多 6 项的 episode-local
value register；它不读取 OCR/UI tree/evaluator，不计算乘积，不增加模型调用，也不
override 动作。R15 只修复了 A1-R14 正式 live 中实际出现的两种语句形态，正式
身份冻结后通过 19 题、537 步零生成 replay：仅 BrowserMultiply 激活，六个 R2
历史成功任务全部静默，离线目标序列为 `[1, 8, 10, 7, 2]`。

冻结链：

- parent evidence commit：`b62839a38f4ae2fc000b0e4a0562a59032e60c5b`
- implementation commit：`c21cad1d5456c37cf72fa677d5fa08d2d8f28665`
- zero-generation preflight commit：`2e99a55496e9ab36e8f61502c1418d98508bc21b`
- live receipt commit：`743dc635670b5a3c15eac4b16c441f879c0307ae`
- source-freeze content SHA：`31e96874b38bc586b1f1a1b705d6477f0e095b883365130d45a36dc8122017fe`
- preflight content SHA：`3f3eca08a1dde0648d414d921bbab6d7a440f48198c069fcc2c619ab8cce8aaf`
- receipt content SHA：`256e48b711f0df4eeb84266301c4bcc957b14c15730e52be83fed7c68fdfcf9a`

正式 target-first live 只运行 BrowserMultiply，并自然结束：

- evaluator reward：`1.0`，success=`true`
- normal model calls：`21`；executed actions：`20`
- infrastructure-invalid attempts：`0`；每次 transport attempts=`1`
- EVR activation=`1`，append=`2`，response append=`2`
- retained values：`[8, 2]`
- rendered EVR reads：`0`；exact-five read=`false`
- checkpoint：`stopped_target_gate_failure`
- 剩余 18 题：`NOT_RUN_BY_PROTOCOL`

因此这不是 R15 机制成功，也不是 R15 导致 Browser 成功。正式分类是
`TARGET_SUCCESS_WITHOUT_MATURE_EVR_READ_UNATTRIBUTED`：任务成功发生在没有任何
EVR 非空读取的轨迹上，只能记为性能成功、机制未观察。按用户冻结边界，不释放
六题保护门或剩余 12 题，不重跑，不以新 live 语句继续补 R16/R17。CPU-only 深度
封存核验了 episode/checkpoint/run-signature/result/source-freeze/preflight/receipt、
single transport、usage 与原始 event/artifact 哈希；正式证据为：

- `evidence/a1r15/A1R15_EOVR_TERMINAL_RESULT_2026-08-18.json`
- terminal evidence content SHA：`d4a7337744f940422e880a1db5a11c586fd08aab45826da535838888096bca52`
- terminal evidence file SHA：`7059f46452e30e977f0cdec76111bb66a089d5e2e9e458800d7b0f00f628a4ab`

### R13–R15 递进结论

- A1-R13：首个 Expense 保护题 valid failure，EVR 全程 silent；不能归因于 EVR。
- A1-R13D：Browser target-first valid failure，模型说出数字但冻结 Action-prefix
  合同没有 exposure；触发合同被否定。
- A1-R14：Browser valid failure，只接受值 `7`、无成熟读取；response grammar 覆盖不足。
- A1-R15：Browser 成功，但只接受 `[8,2]`、无成熟读取；成功不能归因，且继续按
  单条 live 语言补 parser 已失去研究价值。

纯记忆线给出的正面结论不是“记忆无用”，而是：A1-R2 的 pending/unconfirmed
ledger 是当前唯一获得完整 19 题净增益的记忆内核；但被动保存事实只有在模型稳定
外化、状态持续激活且后续决策真正读取时才可能发挥作用。R13–R15 依次暴露了
exposure、语言覆盖和 baseline-ledger coupling 三个瓶颈。下一阶段不得继续沿这一
Browser regex 补丁链，而应在冻结 R2 上比较更明确的复合能力。

### 当前最强完整结果与下一设计任务

- A0：`4/19`，reward `4.5`，329 calls，1,273,361 tokens。
- A1：`5/19`，reward `5.5`，603 calls，3,464,267 tokens；唯一新增成功为
  `RecipeDeleteMultipleRecipesWithConstraint`，但成本显著膨胀。
- A1-R2：`6/19`，是当前最优完整 A-class / pure-memory 结果；后续 R3–R12
  均未通过冻结能力门或未形成可归因提升。
- SYS-NAG V4：`6/19`、reward `6.5`、558 calls、544 actions、2,677,717 tokens；
  是当前最强完整复合系统结果，但增益仍不足以达到预注册的至少 `7/19` 且
  `reward>6.5`。

下一步只允许先做设计，不自动实现：让全新 GPT Pro 审计 A0–A12、A1-R2、
R3–R12、SYS-TRRC、SYS-NAG V4 和 R13–R15，并在 `failure recovery`、
`long-horizon coordination`、`outcome/completion judgment` 三个方向及一个更小替代
之间只冻结一条 implementation-ready 推荐。首要约束是 R2 六个成功任务 0 loss；
主要性能门是至少 `7/19` 且 reward `>6.5`。所有 19 题均已被观察，任何后续结果
只能称 matched prospective/exploratory diagnostic，不能称 held-out generalization。

> 更新时间：2026-08-16（Asia/Hong_Kong）
> 本地主要仓库：`D:\ZJU\Summer_Camp\RAVEN-M-A1`
> GitHub：<https://github.com/ScottBlizzard/RAVEN-M.git>
> 当前分支：`a2-verified-progress-audit-20260810`
> SYS-TRRC-v2 冻结实现提交：`bf2265a30a0ae0400f3f1a6fe6df2828163fc5e7`
> 当前状态：SYS-TRRC-v1 已终止且不得续跑；独立 SYS-TRRC-v2 已完成全部无卡部署、四臂零生成预检，等待新 GPU 进程与 fresh live receipts。**目前尚未发生任何 V2 live generation。**

## 2026-08-15 最高优先级状态覆盖

本节覆盖本文所有较早的“当前状态”描述。

### SYS-TRRC-v2：已完成无卡资格化，尚未开跑

V1 的 Full-L1 曾产生一条内容有用、transport 成功的三字段 auxiliary response，但字段之间含纯空行，V1 strict parser 因而拒绝，advice 没有注入 executor。V2 是独立 identity，唯一机制级变化是：验证前丢弃纯空白行；剩余非空行仍必须恰好三行、固定顺序、固定键、无额外 prose 或 multiline continuation。R2、detector、aux prompts、wrapper、token/latency/call budgets 和任务 seed 均未改变。

- protocol：`SYS_TRRC_R2_ONE_SHOT_RECOVERY_PREREG_V2`
- system：`sys_trrc_r2_one_shot_triggered_recovery_v2`
- implementation commit：`bf2265a30a0ae0400f3f1a6fe6df2828163fc5e7`
- V2 detector replay：`PASS`，`generation_calls=0`，8 个失败题触发、6 个 R2 成功题 0 触发，activation task 仍是 `BrowserMultiply`
- Base / Detector / Generic / Full preflight：全部 `PASS`、`generation_calls=0`、`live_generation_authorized=true`
- 8 个冻结 PNG × Generic/Full 的 exact AutoProcessor 多模态投影：最大 `3169` tokens，低于 `8192` 上限
- V1 真实 aux response 回归：`PASS`，response SHA-256 `5f8c63ee635b460ce38f61dc00c67bf3eb9ed0b8d43c94decaf9e8ae404278a1`

V2 证据入口：

- `protocols/SYS_TRRC_R2_ONE_SHOT_RECOVERY_V2_PREREG_2026-08-15.md`
- `evidence/sys_trrc_v2/SYS_TRRC_V2_R2_DETECTOR_REPLAY.json`
- `evidence/sys_trrc_v2/SYS_TRRC_V2_{BASE,DETECTOR,GENERIC,FULL}_SOURCE_FREEZE.json`
- `evidence/sys_trrc_v2/SYS_TRRC_V2_{BASE,DETECTOR,GENERIC,FULL}_ZERO_GENERATION_PREFLIGHT.json`

固定 live 顺序改为资源节约型 Full-first：`F1 → B1 → D1 → G1 → F2 → G2 → F3 → B3 → D3 → G3 → F4 → G4`。下一步只能在新 GPU 进程启动后，为四臂分别签发 fresh receipt，然后从 `Full-L1 / ExpenseDeleteMultiple2` 开始；若 Full-L1 科学失败，整个 V2 campaign 立即终止，不能再跑 controls 或后续任务。

### SYS-TRRC-v1：正式负结果，永久封存

`SYS-TRRC-v1` 不是新记忆臂，而是 **冻结 A1-R2 memory + 触发式同模型一次性辅助恢复推理**。四臂 Base / Detector / Generic / Full 的零生成预检和 live receipt 均通过；正式 campaign 执行到固定顺序第 4/12 项后终止：

1. Base-L1：Expense `0/1`，34 normal calls，0 aux；
2. Detector-L1：Expense `1/1`，18 normal calls，0 aux；
3. Generic-L1：Expense `1/1`，19 normal calls，0 aux；
4. Full-L1：Expense `0/1`，34 normal calls + 1 aux，终态 `TERMINAL_SCIENTIFIC_FAILURE_STOPPED_PRESERVATION_GATE_FAILURE`。

Full 的 detector 确实触发一次，auxiliary transport 也成功，但模型输出在冻结三字段之间加入空行，被严格 schema 拒绝；`injection_committed_count=0`。所以不能说 recovery advice 导致失败，也不能说 recovery 有效；只能裁定完整系统没有通过保护门，且辅助接口可靠性失败。不得继续 L2/L3/L4、不得改 parser 后沿用同 identity。

正式报告与证据入口：

- `evidence/sys_trrc/SYS_TRRC_CAMPAIGN_TERMINAL_REPORT_2026-08-15.md`
- `evidence/sys_trrc/SYS_TRRC_CAMPAIGN_LEDGER_TERMINAL_2026-08-15.json`
- `evidence/sys_trrc/SYS_TRRC_LIVE_01_BASE_L1_RESULT.json` 至 `SYS_TRRC_LIVE_04_FULL_L1_RESULT.json`
- `evidence/sys_trrc/SYS_TRRC_FULL_L1_EXPENSE_EPISODE_2026-08-15.json`

原始 PNG/UI/event/checkpoint 仍保存在本地 `runs/sys_trrc_*` 与 `runs/sys_trrc_campaign/`（`runs/` 按设计不提交 GitHub）。

---

## 0. 给接手 AI 的第一句话

这是一个以“Qwen3-VL-32B 驱动的 Mobile-use Agent 在 AndroidWorld 长程 Hard 任务中的记忆机制”为主题的浙大夏令营课题。

项目早期已经完成夏令营要求的文献调研、AndroidWorld 部署、Qwen baseline、记忆框架原型、报告与汇报材料。后期为了把工作做得更像真正科研，又连续设计了 A0–A7 多个实验臂。但实验推进过程中发生过两类严重问题：

1. 早期没有完全对齐 Qwen 官方 mobile-agent prompt/action protocol，导致若干旧结果不能解释为纯模型能力；
2. 后期为了追求“至少三个不同记忆机制都跑满 19 题”，错误取消了“先保住 A0 四个成功任务，才能扩展完整 19 题”的能力门，造成 A6 在前期明显退化后仍跑满 19 题，浪费了 GPU 时间。

接手后不要继续堆 A8/A9，也不要马上恢复 A7。先读完本文，核验数据，再与用户确认下一步。用户现在最看重的是：

- 不再浪费 GPU；
- 不把基础设施失败当科学失败；
- 不把“记忆代码运行了”冒充“记忆有效”；
- 不因为想凑齐三种完整机制而强行跑满 19 题；
- 所有扩展实验必须先通过 A0 四个成功任务的 4/4 能力保持门。

---

## 1. 夏令营原始任务是什么

原始任务书：

- `assessment/夏令营考核题目.pdf`
- `assessment/夏令营考核题目_提取文本.txt`
- 工作区原件：`D:\ZJU\Summer_Camp\夏令营考核题目.pdf`

课题三的核心要求是：

1. 调研 GUI Agent / Mobile-use Agent 的记忆机制；
2. 基于 `Qwen3-VL-32B-Instruct` 搭建端到端 GUI Agent baseline；
3. 在 AndroidWorld Hard tasks 上测试 baseline；
4. 设计含记忆机制的框架，比较成功率、提升百分比及典型案例；
5. 提交代码、实验报告、系统架构与复现材料；
6. Medium 扩展是可选，不是当前首要任务。

任务书提到的记忆方向包括：轨迹缓存、结构化状态、知识图谱、层次化记忆、自演化记忆。Planner / Executor / Memory Manager / Critic 是任务书中的建议性框架，不是必须全部使用的硬性要求。真正需要回答的是：不同记忆机制是否能改善长程任务，以及改善来自哪里、代价是什么。

---

## 2. 当前研究问题与正确比较方式

当前最干净的问题不是“如何造一个最复杂的多智能体系统”，而是：

> 在同一个官方 Qwen3-VL-32B mobile-agent controller、同一批 19 个 AndroidWorld Hard 实例、同一 task seed 下，增加不同类型的记忆后，是否能在不破坏原有能力的前提下提高成功率或降低失败成本？

固定条件：

- 模型：`Qwen/Qwen3-VL-32B-Instruct`
- revision：`0cfaf48183f594c314753d30a4c4974bc75f3ccb`
- 推理：单卡 BF16 vLLM
- AndroidWorld：19 个 Hard task instances
- 配对 task seed：`20260806`
- generation seed：`3407`
- 模型可见输入：当前截图
- evaluator：仅在任务结束后打分，对模型隐藏
- 动作协议：Qwen 官方 mobile-agent prompt/action schema 的 AndroidWorld 适配

必须区分三层证据：

1. **机制实现证据**：有没有写入、读取、注入下一次真实请求；
2. **行为影响证据**：是否在第一分叉点改变了模型决策；
3. **任务效果证据**：AndroidWorld evaluator 是否最终给分。

`memory active=true` 只属于第一层，不能直接宣称记忆有效。

---

## 3. 用户最新、最高优先级的实验纪律

这一节覆盖旧文档里所有相冲突的规则。

### 3.1 能力保持门必须恢复

A0 在 seed `20260806` 做对的四个任务是：

1. `ExpenseDeleteMultiple2`
2. `RetroSavePlaylist`
3. `SimpleCalendarAddOneEvent`
4. `SportsTrackerTotalDurationForCategoryThisWeek`

任何新记忆臂必须先跑这四题，并满足 **4/4 evaluator reward = 1**，才有资格扩展到完整 19 题。

- 任一题科学失败：该版本立即停止，不能继续跑剩余门槛题或完整 19 题；
- 只有明确的基础设施无效（连接中断、ADB/UIAutomator、reset/evaluator/lifecycle 错误等）才允许保留痕迹后重试当前题；
- 模型自己答错、格式错、走满预算、错误终止都属于科学失败，不得重跑；
- prompt、代码、阈值、容量或检索规则改变后必须新建版本，从门槛第一题重新开始；
- `RecipeDeleteMultipleRecipesWithConstraint` 是 A1 新增成功，可作为额外观察题，但不是 A0 的四项能力保持条件。

### 3.2 禁止继续沿用 A678 的“无门槛跑满”规则

`protocols/A678_INTEGRATION_PLAN.md` 第 3.8、3.11 和第 6 节把四个 A0 成功任务降成“nonblocking diagnostic”，并明确要求 reward 失败也跑满 19 题。这是造成 A6 浪费的直接协议错误。

该文件仅作为历史记录保留，**不得作为新实验的 continuation policy**。

### 3.3 当前禁止自动执行的事项

- 不自动恢复 A7；
- 不启动 A8；
- 不设计 A9 后马上开 GPU；
- 不为了“凑够三种完整机制”让一个退化臂跑满 19；
- 不在旧 A3–A7 结果上覆盖文件或重新标注；
- 不把 infrastructure-invalid 重试混入正式成本；
- 不复用旧 live receipt 启动新的 vLLM 进程。

---

## 4. 真实实验账本：A0–A7

### 4.1 A0：官方 Qwen mobile baseline

配对 seed `20260806`：

- 成功：`4/19`
- 模型调用 / steps：`329`
- 总 token：`1,273,361`
- wall time：约 `1.82 h`

三 seed 完整诊断（20260806/07/08）：

- 有效任务：`57/57`
- full success：`7/57`
- eligible model calls：`1175`
- episode wall time：约 `471.9 min`

注意：A0–A7 的严格配对比较以 seed `20260806` 的 `4/19` 为主；`7/57` 用于观察随机性和任务稳定性，不能与单 seed 结果直接混写。

必读证据：

- `evidence/baseline/official_qwen32b_full_hard_combined_corrected_final.md`
- `evidence/baseline/official_qwen32b_full_hard_failure_taxonomy_2026-08-08.md`
- `evidence/baseline/official_qwen32b_full_hard_case_notes_2026-08-08.md`

### 4.2 A1：简单 Action Working Memory

机制：模型每步输出 `MEMORY[observed; verified; pending]`，episode 内最多保存六条并回注；无额外模型调用。

结果：

- 成功：`5/19`
- 相对 A0：`+1 gain / 0 loss / 18 ties`
- 新增成功：`RecipeDeleteMultipleRecipesWithConstraint`
- 调用：`603`（A0 为 329）
- token：`3,464,267`（A0 为 1,273,361）
- wall time：约 `4.06 h`
- 记忆写入：`515`
- 非空读取：`580`

结论：A1 给出一个真实但很昂贵的正向信号；它帮助模型记住“仍待确认的重复删除”，但也会稳定错误循环，成本效益很差。

必读：

- `evidence/a1/A1_ACTION_WORKING_MEMORY_RESULTS_2026-08-10.md`
- `protocols/A1_ACTION_WORKING_MEMORY_PREREG_2026-08-10.md`
- 精确 A1 代码 commit：`fbc25dc`

### 4.3 A2-v1r1：可信进度记忆 + 极窄重复动作 guard

结果：

- 成功：`0/19`
- 调用：`705`
- token：`3,170,413`
- wall time：`18,850.7 s`（约 5.24 h）
- 19/19 机制均有运行证据
- 705 次调用中，完整格式输出约 159 次，有效写入 157 次
- guard 仅真正影响 1/19，不能解释整体 0/19

退化原因：格式负担、记忆更新过严导致陈旧、一条“最近进度”无法表达多对象集合状态、额外结构干扰官方动作策略。

必读：

- `evidence/a2/A2_SCORED_RESULT_20260811.md`
- `evidence/a2/A2_DESIGN_RATIONALE_AND_A1_REPLAY_2026-08-10.md`
- `protocols/A2_VERIFIED_PROGRESS_MEMORY_PREREG_2026-08-10.md`
- `A2_PRO_AUDIT.md`

### 4.4 A3：zero-shot ConAct / MemGUI folded context 适配

结果：

- 只跑了第一道门槛题 `ExpenseDeleteMultiple2`
- reward：`0`
- calls：`34`
- suite status：`stopped_capability_gate_failure`
- 没有扩展到完整 19 题

主要问题：要求模型同时行动并输出新的 context 字段，格式遵循率低，明显干扰原策略。

证据：

- `runs/a345_public_memory/official_qwen_20260811T024531_2599f985/`

### 4.5 A4：AWM-style frozen donor workflow memory

donor 准备：

- 本地已有 Contacts、Markor 两个独立 non-Hard 成功 donor；
- 后来额外取得 `ExpenseAddSingle` Easy donor 成功；
- donor bank：`evidence/a345/A4_FROZEN_DONOR_WORKFLOW_BANK.json`

评分结果：

- 第一题 `ExpenseDeleteMultiple2` reward `0`
- calls：`34`
- status：`stopped_capability_gate_failure`
- 没有扩展到完整 19 题

严重问题：早期 donor 抽取读取了错误字段，出现大量近似“perform visible done operation”的空泛 workflow；同时用 ExpenseAdd donor 指导 ExpenseDelete，操作方向不匹配。即使代码链路运行，也不能说明 AWM 思路本身无效，只能说明这个适配版本失败。

证据：

- `runs/a345_public_memory/official_qwen_20260811T030457_d9fa1f65/`
- `evidence/a345/A4_DONOR_SOURCE_AUDIT.json`
- `protocols/A4_AWM_DONOR_ACQUISITION_RUNBOOK.md`

### 4.6 A5：HyMEM-inspired 在线视觉符号图

结果：

- 第一题 `ExpenseDeleteMultiple2` reward `0`
- calls：`34`
- status：`stopped_memory_activation_failure`
- 没有扩展到完整 19 题

主要问题：模型几乎不遵守额外 `GRAPH` 输出协议，机制未形成有效写后读；这个版本不是 full HyMEM，也不能用来否定 HyMEM 论文。

证据：

- `runs/a345_public_memory/official_qwen_20260811T032104_f46a67f9/`
- `protocols/A345_PUBLIC_MEMORY_KERNELS_PREREG_2026-08-11.md`

### 4.7 A6：短 transition-attested episodic buffer

机制：controller 自动记录近期 action → visible transition，不要求模型输出额外前缀，但每步向模型注入短记忆。

结果：

- 有效任务：`19/19`
- 成功：`0/19`
- calls：`628`
- executed actions：`625`
- prompt tokens：`2,594,416`
- completion tokens：`80,006`
- total tokens：`2,674,422`
- elapsed：`19,118.59 s`（约 5.31 h）
- memory active：`19/19`
- writes：`625`
- nonempty reads：`609`
- A0 四项能力保持：`0/4`

为什么会错误跑满 19：`A678_INTEGRATION_PLAN.md` 主动取消了 capability gate，要求 reward 失败也继续。这是执行决策错误，不是 runner 偶然漏门。A6 前期已经明显退化，本应停止。

科学结论：短 action-transition 记忆确实被频繁读取，但它与已有 action history 高度重复，增加注意力负担并破坏基础策略；0/19 是一个完整负结果，但获得它的成本没有必要。

证据：

- `runs/a678_memory/official_qwen_20260811T035710_4b246fd9/aggregate.json`
- `runs/a678_memory/official_qwen_20260811T035710_4b246fd9/checkpoint.json`

### 4.8 A7：deterministic goal-item status ledger

机制：从 goal 中用冻结规则抽取显式目标条目，只记录 `pending / attempted_visible_change / attempted_no_visible_change`，绝不声称完成；无额外模型调用、无 guard、无动作覆盖。

当前状态：用户关闭服务器后已经停止，不得自动恢复。

- 有效任务：`7/19`
- 成功：`1/7`
- 有效任务 calls 合计：`196`
- 当前 success 是 `ExpenseDeleteMultiple2`，它本来就是 A0 成功题；尚无新增成功
- 当前 suite status：`stopped_invalid_episode`
- 当前 suite：`runs/a678_memory/official_qwen_20260811T094144_c61c8a37/`

逐题：

| Task | Reward | Calls | active | nonempty reads | successful writes |
|---|---:|---:|---:|---:|---:|
| BrowserMultiply | 0 | 18 | false | 0 | 0 |
| ExpenseAddMultipleFromGallery | 0 | 60 | false | 0 | 0 |
| ExpenseAddMultipleFromMarkor | 0 | 40 | false | 0 | 0 |
| ExpenseDeleteMultiple2 | 1 | 16 | true | 15 | 12 |
| MarkorCreateNoteAndSms | 0 | 18 | true | 17 | 0 |
| MarkorMergeNotes | 0 | 26 | false | 0 | 0 |
| MarkorTranscribeVideo | 0 | 18 | true | 17 | 0 |

解释：

- `ExpenseDeleteMultiple2` 是唯一形成真实“写入—读取—更新”闭环并成功的任务；
- `MarkorCreateNoteAndSms` 与 `MarkorTranscribeVideo` 虽然反复非空读取，但没有成功状态更新，只能算 parse/read active，不能算记忆内容发挥作用；
- 其余四题完全不触发，说明 goal parser 覆盖过窄。

服务器关闭时正在 `OsmAndMarker` 第 4 步，连接断开被记录为基础设施无效；它不计入正式 7 个结果。旧 suite 中总计 4 条 invalid，其中前三条来自此前的模型连接、UIAutomator 和快照基础设施问题，第四条来自用户关闭服务器。

### 4.9 A8

尚未运行。不要启动。

---

## 5. 当前能够诚实得出的研究结论

1. 官方 Qwen3-VL-32B baseline 在配对 seed 上能做对 4/19，说明环境和 controller 不是“全坏”；三 seed 为 7/57，说明 Hard 确实困难且结果有随机性。
2. 简单 A1 记忆可以新增 1 个成功，但代价是 calls +83%、token +172%、时间约 +123%。
3. A2–A7 证明“更多结构、更多验证、更长记忆”非常容易反向伤害 GUI policy。
4. 主要问题不是“模型没有记忆接口”，而是记忆内容与当前决策的绑定、更新时机、覆盖范围、重复上下文和格式负担。
5. 记忆必须只在与当前 app / operation / object / state 都匹配时进入决策；相似度高或同属 Expense 不足以复用。
6. 对长程 GUI Agent，记忆存在三种常见失败：
   - 完全不触发；
   - 触发但内容没有更新；
   - 高频读取却稳定错误循环。
7. 现有数据已经足够写出有价值的负结果和机制分析，不需要强行造出正结果才能完成夏令营。

---

## 6. 代码、协议、证据的建议阅读顺序

### 第一层：先理解任务和当前事实

1. 本文 `HANDOFF_2026-08-11.md`
2. `assessment/夏令营考核题目.pdf`
3. `evidence/baseline/official_qwen32b_full_hard_combined_corrected_final.md`
4. `evidence/a1/A1_ACTION_WORKING_MEMORY_RESULTS_2026-08-10.md`
5. `evidence/a2/A2_SCORED_RESULT_20260811.md`

### 第二层：理解 A1/A2 的设计与失败

6. `evidence/a2/A2_DESIGN_RATIONALE_AND_A1_REPLAY_2026-08-10.md`
7. `A2_PRO_AUDIT.md`
8. `protocols/A1_ACTION_WORKING_MEMORY_PREREG_2026-08-10.md`
9. `protocols/A2_VERIFIED_PROGRESS_MEMORY_PREREG_2026-08-10.md`
10. `implementation/src/raven_m/official_qwen_mobile/progress_memory.py`
11. `implementation/src/raven_m/official_qwen_mobile/controller.py`

### 第三层：理解公开记忆适配与后期错误

12. `PUBLIC_FRAMEWORK_DECISION.md`（工作区根目录，不在本仓库）
13. `protocols/A345_PUBLIC_MEMORY_KERNELS_PREREG_2026-08-11.md`
14. A3/A4/A5 三个 suite 的 `checkpoint.json` 与首题 `episode.json`
15. `implementation/src/raven_m/official_qwen_mobile/a345_memory.py`
16. `protocols/A678_INTEGRATION_PLAN.md`——**只读历史，特别注意它取消 gate 的决策已被废止**
17. `implementation/src/raven_m/official_qwen_mobile/a678_memory.py`
18. `implementation/src/raven_m/official_qwen_mobile/a678_contract.py`
19. A6 aggregate 与 A7 checkpoint

### 第四层：理解官方 controller 与运行链

20. `implementation/src/raven_m/official_qwen_mobile/protocol.py`
21. `implementation/scripts/run_official_qwen_mobile.py`
22. `implementation/scripts/run_a678_arm.py`
23. `implementation/src/raven_m/models/vllm_client.py`
24. `implementation/src/raven_m/env/androidworld_adapter.py`
25. `implementation/configs/androidworld_hard_v2_instances.json`

---

## 7. Git 与代码状态

本地：

```text
repo:   D:\ZJU\Summer_Camp\RAVEN-M-A1
branch: a2-verified-progress-audit-20260810
HEAD:   dd3f2bc32eac12a1a02d929e30f7fa75f9580175
```

远端：

```text
https://github.com/ScottBlizzard/RAVEN-M.git
origin/a2-verified-progress-audit-20260810 = be1f1cd388c396b2c6d3d443f9dc353bbe35655c
```

因此本地至少领先远端一个 commit：

```text
dd3f2bc Fix canonical scored-arm resume signature comparison
```

该 commit 修复 resume 时 JSON list 与内存 tuple 直接比较造成的假签名不匹配。

当前 `git status --short` 中已知未跟踪项：

```text
?? 06_local_runtime/
?? evidence/a345/A345_LIVE_SERVER_RECEIPT.json
```

`06_local_runtime/` 是大型本地环境/junction，绝对不要 `git add -A`。不要随意提交旧 live receipt；进程重启后它本来就会失效。

本交接文档创建后也会显示为未跟踪，除非用户明确要求提交/推送。

---

## 8. GPU 服务器与 SSH

最后一次使用的实例：

```text
ssh -p 13690 root@connect.westd.seetacloud.com
```

本地私钥：

```text
C:\Users\lenovo\.ssh\autodl_raven_m
```

推荐显式连接：

```powershell
ssh -i C:\Users\lenovo\.ssh\autodl_raven_m -p 13690 root@connect.westd.seetacloud.com
```

重要：云实例克隆或重建后 host/port 可能变化，连接前先问用户或看平台页面。不要把旧地址视为永久地址。

最后已知远端路径：

```text
repo:        /root/autodl-tmp/RAVEN-M-A345
model:       /root/autodl-tmp/models/Qwen3-VL-32B-Instruct-modelscope
python env:  /root/autodl-tmp/envs/qwen_vllm
served name: Qwen/Qwen3-VL-32B-Instruct
port:        18000
```

最后一次 vLLM 实际命令对应：

```bash
/root/autodl-tmp/envs/qwen_vllm/bin/vllm serve \
  /root/autodl-tmp/models/Qwen3-VL-32B-Instruct-modelscope \
  --served-model-name Qwen/Qwen3-VL-32B-Instruct \
  --host 127.0.0.1 \
  --port 18000 \
  --tensor-parallel-size 1 \
  --dtype bfloat16 \
  --gpu-memory-utilization 0.92 \
  --max-model-len 65536 \
  --max-num-seqs 1 \
  --limit-mm-per-prompt '{"image":1}' \
  --generation-config vllm \
  --no-enable-log-requests
```

但服务器现在已经关闭，旧 PID `1334` 和以下旧回执均失效：

- `evidence/a678/A678_LIVE_SERVER_RECEIPT.json`
- `evidence/a345/A345_LIVE_SERVER_RECEIPT.json`

重新开机后必须重新核验模型文件、实际 PID/command line、vLLM/torch/transformers 版本、served model ID，并生成新的 live receipt。不能只看到 `/v1/models` 同名就复用旧回执。

SSH 隧道示例：

```powershell
ssh -i C:\Users\lenovo\.ssh\autodl_raven_m \
  -p 13690 \
  -N \
  -L 18000:127.0.0.1:18000 \
  root@connect.westd.seetacloud.com
```

当前隧道已经停止。

---

## 9. 本地 AndroidWorld 环境

Python：

```text
D:\ZJU\Summer_Camp\RAVEN-M-A1\06_local_runtime\envs\androidworld\Scripts\python.exe
```

ADB：

```text
D:\ZJU\Summer_Camp\RAVEN-M-A1\06_local_runtime\android\sdk\platform-tools\adb.exe
```

AndroidWorld source：

```text
D:\ZJU\Summer_Camp\RAVEN-M-Research\03_code\third_party\android_world
```

AVD / ports：

```text
AVD: AndroidWorldAvd
ADB console: emulator-5554 / 5554
gRPC: 8554
resolution: 1080 x 2400
```

启动/停止：

```powershell
& .\06_local_runtime\scripts\start_emulator.ps1 -BootTimeoutSeconds 360
& .\06_local_runtime\scripts\stop_emulator.ps1
```

### 9.1 2026-08-11 出现过的冷启动故障

冷启动后出现：

- ADB 显示 device；
- `sys.boot_completed=1`；
- 但屏幕黑，只有 pointer-location 调试条；
- ActivityManager `mSleeping=true`；
- UIAutomator 报 `null root node`；
- `/data/data/android_world/snapshots` 消失。

根因之一是 `com.android.systemui/.SystemUIService` 没有运行。恢复命令：

```powershell
$adb='D:\ZJU\Summer_Camp\RAVEN-M-A1\06_local_runtime\android\sdk\platform-tools\adb.exe'
& $adb -s emulator-5554 shell am startservice --user 0 -n com.android.systemui/.SystemUIService
```

随后成功用已有受审计脚本重建 11 个 scored-app snapshots：

```powershell
& 'D:\ZJU\Summer_Camp\RAVEN-M-A1\06_local_runtime\envs\androidworld\Scripts\python.exe' \
  .\prepare_mobileuse_c0_snapshots.py \
  --adb-path 'D:\ZJU\Summer_Camp\RAVEN-M-A1\06_local_runtime\android\sdk\platform-tools\adb.exe'
```

该命令的工作目录是：

```text
D:\ZJU\Summer_Camp\RAVEN-M-PF01\implementation\scripts
```

最终 11/11 snapshot pass，generation calls = 0。不要把这套重建当作每次开机都要重复的仪式；只有快照确实缺失/损坏时才做。

---

## 10. 如果未来用户明确同意恢复 A7

默认不要执行本节。只有用户明确批准后才可使用。

原 suite：

```text
runs/a678_memory/official_qwen_20260811T094144_c61c8a37
```

原恢复命令：

```powershell
& '.\06_local_runtime\envs\androidworld\Scripts\python.exe' \
  '.\implementation\scripts\run_a678_arm.py' \
  --arm a7 \
  --adb-path '.\06_local_runtime\android\sdk\platform-tools\adb.exe' \
  --launch-receipt '.\evidence\a678\A678_LIVE_SERVER_RECEIPT.json' \
  --resume-suite-dir '.\runs\a678_memory\official_qwen_20260811T094144_c61c8a37' \
  --execute
```

但是：

1. 旧 receipt 已失效，必须重新生成；
2. 旧 A7 continuation policy 没有能力门，继续跑会延续错误协议；
3. 当前用户已经要求停止，并对无门槛扩展非常不满；
4. 所以更合理的是保留 A7 为 partial diagnostic，不恢复。

---

## 11. 正式报告与汇报材料在哪里

最终交付包：

```text
D:\ZJU\Summer_Camp\夏令营课题三_最终交付包_2026-08-03
```

正式 LaTeX 报告：

```text
D:\ZJU\Summer_Camp\夏令营课题三_最终交付包_2026-08-03\10_LaTeX正式报告\main.tex
D:\ZJU\Summer_Camp\夏令营课题三_最终交付包_2026-08-03\10_LaTeX正式报告\main.pdf
```

中期汇报：

```text
D:\ZJU\Summer_Camp\夏令营课题三_最终交付包_2026-08-03\12_中期汇报_2026-08-07
```

其中包括 PPT 与多版讲稿。

重要：正式报告最后更新约在 2026-08-09，**尚未完整、准确吸收 A1–A7 的最终结果**。不要把报告里的旧实验表直接当当前事实。未来更新应以本交接文档和 raw suite 为准，并明确：

- A1 是唯一正向但昂贵的完整记忆臂；
- A2、A6 是完整负结果；
- A3/A4/A5 是门槛失败，不是 19 题结果；
- A7 是 partial 7/19，不得补成完整结果；
- A8 未运行；
- 不得把公开方法“适配失败”写成公开方法本身无效。

---

## 12. 哪些旧资料只适合当背景，不应再控制实验

工作区存在大量早期规划与 GPT Pro 讨论：

- `D:\ZJU\Summer_Camp\RAVEN-M_GPTPro_answer.md`
- `D:\ZJU\Summer_Camp\RAVEN-M_新主线研究路线图_2026-08-03.md`
- `D:\ZJU\Summer_Camp\GPTPro_新方向请求_2026-08-05.md`
- `D:\ZJU\Summer_Camp\PUBLIC_FRAMEWORK_DECISION.md`
- `D:\ZJU\Summer_Camp\A2_PRO_AUDIT.md`
- `D:\ZJU\Summer_Camp\RAVEN-M-Research\2026_08_05_17_36.md`
- `D:\ZJU\Summer_Camp\RAVEN-M-Research\2026_08_05_20_14.md`

这些资料记录了思考演进，适合写报告中的研究过程，但很多判断发生在官方 Qwen baseline 完整复现之前，或在 A1/A2 真正数据出来之前。阅读时必须按时间排序，不能让旧设想覆盖新证据。

如果下一步需要 GPT Pro 决策，应把本交接文档、A0/A1/A2结果、A3–A7失败账本和能力门规则一次性提供给它，不要只截取某一条失败路线让它继续局部补丁。

---

## 13. 建议下一位 AI 的第一轮工作

在不打开 GPU 的前提下：

1. 读本文与第 6 节列出的核心证据；
2. 用脚本重新汇总 A0–A7 的 success/calls/tokens/time/activation，不手抄旧报告数字；
3. 对 A1 唯一 gain、A2/A6 全面退化、A3/A4/A5 首题失败、A7 partial 做 first-divergence 对比；
4. 判断是否已经足够完成夏令营最终报告；答案大概率是“足够，而且数据比中期时丰富很多”；
5. 如果还要做新实验，只提出一个最小新臂，并先证明：
   - 不改变官方 system prompt/action schema；
   - 不要求模型额外输出 memory 前缀；
   - 不重复注入已有 action history；
   - 只在严格匹配的状态下触发；
   - 额外 calls 为 0，或提供 call-matched control；
   - 四项 A0 preservation gate 为机器强制；
6. 把设计和预计 GPU 成本先与用户确认，再写代码或开机。

一个更稳妥的选择是：不再追求新 accuracy arm，转而把现有结果整理成“记忆何时帮助、何时反向伤害”的系统分析。这与夏令营要求完全一致，也比继续盲跑更有科研价值。

---

## 14. 接手 AI 绝对不要做的事

- 不要说“为了科学完整性所以失败也跑满”，除非用户重新明确授权；
- 不要为了满足“至少三种机制”而把明显失败的版本跑 19 题；
- 不要同时改 memory、history、prompt、guard、planner 后声称是记忆提升；
- 不要在 GPU 开着时临时大改代码；
- 不要把测试通过等同于机制有效；
- 不要把 nonempty read 等同于 causal benefit；
- 不要把模型终止成功等同于 evaluator 成功；
- 不要重跑科学失败；
- 不要因 infrastructure-invalid 而丢弃旧痕迹；
- 不要覆盖 frozen 结果；
- 不要提交 `06_local_runtime/`；
- 不要复用旧 live receipt；
- 不要未经用户允许 push GitHub 或修改正式报告；
- 不要继续控制其他旧 Codex 对话框。

---

## 15. 一段话总结整个项目

本项目已经完成了夏令营要求的 Qwen3-VL-32B AndroidWorld Hard baseline、记忆机制调研、多个记忆原型、分层日志、案例分析和报告框架。最可靠的配对结果是：A0 为 4/19；A1 简单工作记忆提高到 5/19，但调用、token 和时间显著增加；A2 的可信进度记忆退化到 0/19；A3/A4/A5 三种公开记忆思路的轻量适配均在第一道能力保持题失败；A6 的短转移记忆被错误地无门槛跑满，最终 0/19；A7 目前 1/7，唯一成功只是保住 A0 已会任务，尚无新增成功。现阶段最重要的不是继续堆机制，而是恢复 4/4 能力门，诚实分析“为什么记忆越加越差”，并把这种负结果转化为关于格式负担、状态绑定、更新时机、上下文冗余和错误循环的研究结论。
